# 模块2.2：交互模式与工作流

## 本节目标

掌握Claude Code的高级交互模式和多种工作流，学会：
- 深度使用交互式REPL模式的高级功能
- 理解和应用Thinking Mode的4个层级
- 掌握Rewind功能进行会话回溯
- 使用Headless模式实现自动化
- 通过SDK集成Claude Code到自己的工具
- 配置CLAUDE.md实现智能上下文管理
- 选择合适的工作流模式解决实际问题

---

## 第1部分：交互模式深度使用（3,000字）

### 1.1 REPL模式核心概念

Claude Code的交互式REPL（Read-Eval-Print Loop）模式是最常用的工作方式，但其功能远超简单的"问答对话"。

#### 1.1.1 REPL工作原理

```bash
# 启动REPL模式
claude

# 工作循环
┌─────────────────────────────────────┐
│ 1. Read    - 读取用户输入           │
│ 2. Eval    - AI理解并规划            │
│ 3. Print   - 输出结果或执行工具      │
│ 4. Loop    - 等待下一轮输入          │
└─────────────────────────────────────┘
```

**关键特性**：
- **有状态对话**：每轮对话都记住之前的上下文
- **工具执行**：可以调用文件操作、命令执行等工具
- **流式输出**：实时显示AI的思考和操作过程
- **中断与恢复**：支持Ctrl+C中断，支持会话恢复

#### 1.1.2 输入模式切换

Claude Code支持3种输入模式：

**单行输入模式**（默认）
```bash
You: 列出当前目录的文件
```

**多行输入模式**（按Enter后继续输入）
```bash
You: 帮我重构这段代码：
... def process_data(data):
...     result = []
...     for item in data:
...         if item > 0:
...             result.append(item * 2)
...     return result
...
... # 空行结束输入
```

**文件输入模式**
```bash
You: /add path/to/file.py
You: 分析这个文件的性能问题
```

**快捷键总览**：
- `Enter` - 单行模式直接发送，多行模式换行
- `Shift+Enter` - 强制换行（单行模式）
- `Ctrl+D` - 发送多行输入（Unix）
- `Ctrl+Z` + `Enter` - 发送多行输入（Windows）
- `Escape` - **Rewind功能**（回退到上一步）
- `Ctrl+C` - 中断当前操作
- `Ctrl+D` - 退出REPL

### 1.2 Thinking Mode深度解析

Thinking Mode是Claude Code最强大的功能之一，让AI在回答前进行**可见的深度思考**。

#### 1.2.1 四个思考层级

| 模式 | 命令 | 思考深度 | 适用场景 | Token消耗 |
|------|------|---------|----------|----------|
| **基础思考** | `think` | ⭐ | 简单重构、代码审查 | 1x |
| **深度思考** | `think hard` | ⭐⭐⭐ | 架构设计、复杂算法 | 3x |
| **极限思考** | `think harder` | ⭐⭐⭐⭐⭐ | 系统重构、性能优化 | 5x |
| **超级思考** | `ultrathink` | ⭐⭐⭐⭐⭐⭐⭐ | 架构迁移、安全审计 | 10x |

#### 1.2.2 Thinking Mode实战对比

**场景1：代码重构**

```bash
# 普通模式（无思考）
You: 重构这个函数让它更简洁
Claude: 好的，我会使用列表推导式...
```

```bash
# 基础思考模式
You: think 重构这个函数让它更简洁
Claude:
<thinking>
让我分析一下这个函数：
1. 当前逻辑是遍历+条件+追加
2. 可以用列表推导式简化
3. 但要保持可读性...
</thinking>

我建议这样重构...
```

```bash
# 深度思考模式
You: think hard 重构这个函数让它更简洁
Claude:
<thinking>
第一层分析：
- 函数功能是过滤+转换
- 时间复杂度O(n)
- 空间复杂度O(n)

第二层优化：
- 列表推导式 vs filter+map
- 性能对比：列表推导式更快
- 可读性对比：列表推导式更清晰

第三层权衡：
- 如果数据量巨大，考虑生成器
- 如果需要链式调用，考虑itertools
- 当前场景：数据量中等，列表推导式最佳
</thinking>

基于深度分析，我建议...
```

#### 1.2.3 何时使用哪种思考模式

**think（基础思考）**
- ✅ 简单代码重构（<50行）
- ✅ 添加注释或文档
- ✅ 修复明显的bug
- ✅ 文件结构调整

**think hard（深度思考）**
- ✅ 复杂函数优化
- ✅ 架构设计决策
- ✅ 性能瓶颈分析
- ✅ 安全性审查
- ✅ 算法选择

**think harder（极限思考）**
- ✅ 大规模重构（>1000行）
- ✅ 系统架构迁移
- ✅ 并发模型设计
- ✅ 内存优化方案
- ✅ 跨模块依赖解耦

**ultrathink（超级思考）**
- ✅ 整个系统的架构重设计
- ✅ 安全漏洞深度审计
- ✅ 性能优化（10x级别）
- ✅ 遗留系统现代化改造
- ✅ 分布式系统设计

**实战案例：选择合适的思考模式**

```python
# 场景1：简单重构 → think
def get_user(id):
    user = db.query("SELECT * FROM users WHERE id=?", id)
    return user

# 场景2：性能优化 → think hard
def process_large_dataset(data):
    # 需要处理1000万条记录
    # 当前实现很慢，需要优化
    results = []
    for item in data:
        if complex_condition(item):
            results.append(transform(item))
    return results

# 场景3：架构重构 → think harder
# 需要将单体应用拆分为微服务
# 涉及数据库拆分、服务通信、事务处理等

# 场景4：安全审计 → ultrathink
# 需要审计整个系统的安全性
# 包括认证、授权、注入、XSS、CSRF等
```

### 1.3 Rewind功能：时光倒流

#### 1.3.1 Rewind工作原理

Rewind功能允许你撤销AI的最后一次操作，回到之前的状态。

```bash
# 工作流程
┌─────────────────────────────────────────┐
│ 状态A → AI操作 → 状态B                   │
│                    ↓                     │
│              按Escape键                   │
│                    ↓                     │
│ 状态A ← 回退 ← 状态B（撤销）             │
└─────────────────────────────────────────┘
```

**Rewind可以撤销什么**：
- ✅ 文件修改（Edit工具）
- ✅ 文件创建（Write工具）
- ✅ 文件删除
- ✅ 命令执行（Bash工具）
- ✅ Git操作（未push的）

**Rewind无法撤销什么**：
- ❌ 已经push到远程的Git提交
- ❌ 已经发送的网络请求
- ❌ 已经执行的外部脚本副作用
- ❌ 已经修改的远程数据库

#### 1.3.2 Rewind实战场景

**场景1：撤销错误的代码修改**

```bash
You: 把所有的print改成log.info
Claude: [执行Edit工具，修改了50个文件]
You: <按Escape键>
Claude: ✅ 已回退，撤销了对50个文件的修改

You: 只修改src/目录下的print语句
Claude: [只修改src/目录]
```

**场景2：撤销危险的命令执行**

```bash
You: 删除所有临时文件
Claude: [准备执行] rm -rf tmp/*
You: <按Escape键>
Claude: ✅ 已回退，未执行删除命令

You: 只删除tmp/目录下的.cache文件
Claude: [执行] rm tmp/*.cache
```

**场景3：Git操作回退**

```bash
You: 提交所有修改
Claude: [执行] git add . && git commit -m "..."
You: <按Escape键>
Claude: ✅ 已回退Git提交

You: 只提交src/目录的修改
Claude: [执行] git add src/ && git commit
```

#### 1.3.3 Rewind的最佳实践

**使用建议**：
1. **高风险操作前要谨慎**：删除文件、大规模修改、Git操作
2. **利用Rewind快速迭代**：不满意就回退，重新指导
3. **配合Thinking Mode**：思考+执行+回退+优化
4. **记录回退原因**：帮助AI理解你的需求

**反模式**：
- ❌ 频繁回退导致会话混乱
- ❌ 回退后不给新指令，重复相同操作
- ❌ 依赖Rewind而不是提前明确需求

### 1.4 会话管理高级技巧

#### 1.4.1 会话持久化

```bash
# 导出当前会话
You: /export my-session.json
Claude: ✅ 会话已导出到 my-session.json

# 恢复会话
claude --resume my-session.json

# 查看历史会话
You: /sessions
Claude:
📋 历史会话列表：
1. [2025-12-10 14:30] 重构用户认证模块 (45分钟)
2. [2025-12-10 10:15] 修复支付系统bug (1小时)
3. [2025-12-09 16:00] 添加日志功能 (30分钟)

You: /resume 1
Claude: ✅ 已恢复会话"重构用户认证模块"
```

#### 1.4.2 上下文管理

**问题**：长时间对话后，上下文会变得混乱

**解决方案**：
1. **定期清理上下文**：`/clear` 清除历史消息
2. **分段式对话**：完成一个任务后用`/clear`，开始下一个
3. **使用文件引用**：`/add file.py` 而不是粘贴代码

```bash
# 示例：分段式对话
You: 帮我重构用户认证模块
Claude: [完成重构]

You: /clear  # 清理上下文
You: /add src/payment.py  # 添加新文件
You: 现在帮我优化支付模块
Claude: [开始新任务，不受之前上下文干扰]
```

#### 1.4.3 智能上下文注入

通过`CLAUDE.md`文件自动注入项目上下文：

```markdown
# 项目：电商系统

## 技术栈
- Python 3.11 + FastAPI
- PostgreSQL + Redis
- Docker + Kubernetes

## 编码规范
- 函数必须有类型注解
- 使用Pydantic做数据验证
- 异步优先（async/await）

## 项目结构
src/
  api/      # API路由
  models/   # 数据模型
  services/ # 业务逻辑
  utils/    # 工具函数
```

**效果**：每次对话开始，Claude自动知道项目结构和规范，无需重复说明。

---

### 1.1 REPL交互模式（核心）

REPL（Read-Eval-Print-Loop）交互模式是Claude Code最核心的使用方式，适合日常开发工作。

**启动方式**：

```bash
# 最简启动
claude

# 指定项目目录
claude --project /path/to/project

# 启用详细日志
claude --verbose

# 指定模型
claude --model claude-opus-4

# 组合选项
claude --verbose --model claude-sonnet-4 --project ~/work/app
```

**交互模式特点**：

1. **持续对话**：保持完整对话历史，AI理解上下文
2. **实时反馈**：即时查看结果，快速迭代
3. **丰富命令**：支持30+ Slash命令
4. **状态保持**：工作目录、配置在会话中保持

**交互模式界面**：

```bash
$ claude

Claude Code v2.0.58
Working directory: /Users/yourname/my-project
Type your message or /help for commands

You:
```

**核心交互流程**：

```
用户输入 → Claude分析 → 执行工具 → 返回结果 → 等待下一轮
    ↑                                              ↓
    └──────────── 保持对话上下文 ←─────────────────┘
```

**最佳实践**：

```bash
# 1. 配置别名简化启动
# 在 ~/.bashrc 或 ~/.zshrc 中添加
alias cc="claude --dangerously-skip-permissions"
alias ccv="claude --verbose"
alias cco="claude --model claude-opus-4"

# 2. 项目专用启动脚本
# ~/projects/my-app/start-claude.sh
#!/bin/bash
cd ~/projects/my-app
claude --project . --model claude-sonnet-4

# 3. 使用tmux保持会话
tmux new-session -d -s claude-dev
tmux send-keys -t claude-dev "claude" Enter
tmux attach -t claude-dev
```

**适用场景**：

| 场景 | 推荐配置 | 原因 |
|------|---------|------|
| 日常开发 | `claude` | 标准交互，平衡效率 |
| 个人项目 | `claude --dangerously-skip-permissions` | 减少确认，加速迭代 |
| 复杂调试 | `claude --verbose` | 详细日志辅助定位 |
| 重要决策 | `claude --model claude-opus-4` | 最强推理能力 |

---

### 1.2 单次执行模式（-p参数）

单次执行模式（Print Mode）适合自动化脚本和快速查询，执行完毕即退出。

**语法**：

```bash
claude -p "your prompt here"
# 或
claude --print "your prompt here"
```

**核心特点**：

1. **无交互**：执行后立即退出
2. **干净输出**：只输出AI响应，无格式修饰
3. **可组合**：完美支持Unix管道
4. **脚本友好**：适合自动化工作流

**对比交互模式**：

```bash
# 交互模式输出
$ claude "What's 2+2?"
Claude Code v2.0.58
Working directory: /home/user
Claude: The answer is 4.

# 单次执行模式输出
$ claude -p "What's 2+2?"
The answer is 4.
```

**自动化脚本示例**：

```bash
#!/bin/bash
# auto-review.sh - 自动代码审查脚本

echo "=== 自动代码审查开始 ==="
TIMESTAMP=$(date +"%Y%m%d_%H%M%S")
REPORT="review_${TIMESTAMP}.md"

# 步骤1：检查代码风格
echo "1. 检查代码风格..."
claude -p "Review all Python files for PEP8 compliance" > style_issues.txt

# 步骤2：安全扫描
echo "2. 安全扫描..."
claude -p "Scan for security vulnerabilities in the codebase" > security_issues.txt

# 步骤3：生成综合报告
echo "3. 生成报告..."
cat << EOF > $REPORT
# 代码审查报告
生成时间: $(date)

## 代码风格问题
$(cat style_issues.txt)

## 安全问题
$(cat security_issues.txt)
EOF

echo "=== 审查完成！报告: $REPORT ==="
```

**条件执行**：

```bash
# 基于AI判断执行后续操作
if claude -p "Are all tests passing? Reply with only 'yes' or 'no'" | grep -q "yes"; then
    echo "Tests passed, deploying..."
    ./deploy.sh
else
    echo "Tests failed, aborting deployment"
    exit 1
fi
```

**批量处理**：

```bash
# 为所有Python文件添加文档字符串
for file in *.py; do
    echo "Processing $file..."
    claude -p "Add comprehensive docstrings to $file"
done

# 批量代码翻译
for file in src/*.js; do
    echo "Converting $file to TypeScript..."
    claude -p "Convert $file to TypeScript with proper types" > "${file%.js}.ts"
done
```

---

### 1.3 Headless无头模式

Headless模式是单次执行模式的进阶版本，专为CI/CD和程序化集成设计。

**语法**：

```bash
claude -p "prompt" --output-format json
claude -p "prompt" --output-format stream-json
```

**输出格式对比**：

```bash
# 普通文本输出
$ claude -p "List files in src/"
There are 5 files in the src directory: app.js, utils.js...

# JSON结构化输出
$ claude -p "List files in src/" --output-format json
{
  "result": {
    "type": "text",
    "content": "There are 5 files in the src directory...",
    "files": ["app.js", "utils.js", "config.js", "index.js", "helpers.js"]
  },
  "usage": {
    "input_tokens": 45,
    "output_tokens": 128
  },
  "session_id": "abc123def456"
}

# 流式JSON输出（实时）
$ claude -p "Analyze codebase" --output-format stream-json
{"type":"thinking","content":"Analyzing project structure..."}
{"type":"tool_use","name":"Read","input":{"file_path":"src/app.js"}}
{"type":"tool_result","content":"file contents here..."}
{"type":"text","content":"The codebase analysis shows..."}
```

**CI/CD集成示例**：

```yaml
# .github/workflows/claude-review.yml
name: AI Code Review

on: [pull_request]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Claude Code
        run: npm install -g @anthropic-ai/claude-code

      - name: Run AI Review
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          # 获取变更文件
          CHANGED_FILES=$(git diff --name-only origin/main)

          # AI审查并输出JSON
          REVIEW=$(claude -p "Review these changed files for issues: $CHANGED_FILES" \
            --output-format json)

          # 解析结果
          echo "$REVIEW" | jq '.result.content' > review.md

      - name: Post Review Comment
        uses: actions/github-script@v7
        with:
          script: |
            const review = require('fs').readFileSync('review.md', 'utf8');
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: review
            });
```

**程序化调用**：

```python
#!/usr/bin/env python3
# claude_automation.py

import subprocess
import json

def run_claude(prompt: str, output_format: str = "json") -> dict:
    """运行Claude Code并返回结构化结果"""
    result = subprocess.run(
        ["claude", "-p", prompt, "--output-format", output_format],
        capture_output=True,
        text=True
    )

    if result.returncode != 0:
        raise Exception(f"Claude error: {result.stderr}")

    return json.loads(result.stdout)

def analyze_codebase(path: str) -> dict:
    """分析代码库并返回报告"""
    result = run_claude(f"Analyze the codebase at {path} and provide a summary")
    return result

def fix_issues(issues: list) -> list:
    """批量修复问题"""
    fixed = []
    for issue in issues:
        result = run_claude(f"Fix this issue: {issue}")
        fixed.append({
            "issue": issue,
            "fix": result["result"]["content"],
            "tokens_used": result["usage"]
        })
    return fixed

# 使用示例
if __name__ == "__main__":
    analysis = analyze_codebase("./src")
    print(json.dumps(analysis, indent=2))
```

---

### 1.4 SDK编程模式

SDK编程模式提供了最强大的控制能力，适合构建自定义Agent和复杂自动化系统。

**安装SDK**：

```bash
# Python SDK
pip install claude-agent-sdk

# TypeScript/Node SDK
npm install @anthropic-ai/claude-agent-sdk
```

**Python SDK基础示例**：

```python
from claude_agent_sdk import Agent, Session

# 创建Agent
agent = Agent(
    system_prompt="""你是一个专业的代码审查专家。
    你的任务是仔细审查代码，找出潜在问题并提供改进建议。
    始终保持建设性和专业性。""",
    model="claude-sonnet-4",
    max_tokens=4096
)

# 创建会话
session = Session(agent)

# 运行任务
result = session.run(
    prompt="审查 src/auth.py 文件的安全性",
    tools=["Read", "Grep", "Glob"],  # 允许使用的工具
    working_directory="./my-project"
)

print(result.content)
print(f"Token使用: {result.usage}")
```

**TypeScript SDK示例**：

```typescript
import { Agent, Session, Tool } from '@anthropic-ai/claude-agent-sdk';

// 配置Agent
const agent = new Agent({
  systemPrompt: `你是一个全栈开发专家。
    你可以帮助用户构建完整的Web应用。`,
  model: 'claude-sonnet-4',
  maxTokens: 8192
});

// 自定义工具
const customTool: Tool = {
  name: 'deploy',
  description: '部署应用到服务器',
  parameters: {
    environment: { type: 'string', enum: ['staging', 'production'] }
  },
  handler: async (params) => {
    // 实际部署逻辑
    console.log(`Deploying to ${params.environment}...`);
    return { success: true, url: `https://${params.environment}.example.com` };
  }
};

// 运行会话
async function main() {
  const session = new Session(agent);

  // 添加自定义工具
  session.addTool(customTool);

  // 执行任务
  const result = await session.run({
    prompt: '创建一个React组件并部署到staging环境',
    streaming: true,  // 流式输出
    onProgress: (event) => {
      console.log(`[${event.type}] ${event.content}`);
    }
  });

  console.log('最终结果:', result.content);
}

main();
```

**构建自定义Agent**：

```python
from claude_agent_sdk import Agent, Session, Tool
from typing import List, Dict, Any

class CodeReviewAgent:
    """专业代码审查Agent"""

    def __init__(self, project_path: str):
        self.project_path = project_path
        self.agent = Agent(
            system_prompt=self._build_system_prompt(),
            model="claude-opus-4",
            max_tokens=16384
        )
        self.session = Session(self.agent)

    def _build_system_prompt(self) -> str:
        return """你是一个资深代码审查专家，具有以下专长：

        1. **安全审查**：识别SQL注入、XSS、CSRF等安全漏洞
        2. **性能分析**：发现性能瓶颈和优化机会
        3. **代码质量**：检查代码风格、可维护性、测试覆盖
        4. **架构评估**：评估设计模式和架构决策

        审查原则：
        - 提供具体、可操作的建议
        - 区分关键问题和改进建议
        - 给出修复代码示例
        - 解释问题的根本原因"""

    def review_file(self, file_path: str) -> Dict[str, Any]:
        """审查单个文件"""
        result = self.session.run(
            prompt=f"详细审查 {file_path} 文件",
            working_directory=self.project_path
        )
        return {
            "file": file_path,
            "review": result.content,
            "issues": self._extract_issues(result.content)
        }

    def review_pr(self, changed_files: List[str]) -> Dict[str, Any]:
        """审查整个PR"""
        reviews = []
        for file in changed_files:
            reviews.append(self.review_file(file))

        # 综合评估
        summary = self.session.run(
            prompt=f"""基于以下文件审查结果，提供PR综合评估：
            {reviews}

            包括：总体评分(1-10)、关键问题列表、是否建议合并"""
        )

        return {
            "file_reviews": reviews,
            "summary": summary.content
        }

    def _extract_issues(self, content: str) -> List[Dict]:
        """从审查内容中提取问题列表"""
        # 实现问题提取逻辑
        pass

# 使用示例
reviewer = CodeReviewAgent("./my-project")
pr_review = reviewer.review_pr(["src/auth.py", "src/api.py"])
print(pr_review["summary"])
```

---

### 1.5 管道模式

管道模式充分利用Unix管道机制，实现数据流处理和命令组合。

**基础管道**：

```bash
# 输入重定向
claude -p "分析这段代码" < code.py

# 输出重定向
claude -p "生成README" > README.md

# 管道传输
cat error.log | claude -p "分析错误原因"

# Here Document
claude -p "分析这段代码" << EOF
function hello() {
    console.log("Hello, World!");
}
EOF
```

**高级管道组合**：

```bash
# 多步处理流水线
cat src/*.py | \
    claude -p "提取所有函数定义" | \
    claude -p "为每个函数生成文档字符串" | \
    tee documented_functions.txt

# 条件管道
git diff HEAD~1 | \
    claude -p "判断是否需要更新测试，只回答yes或no" | \
    grep -q "yes" && \
    claude -p "为最近的代码变更生成测试"

# 并行管道处理
# 使用GNU parallel
find . -name "*.py" | \
    parallel -j4 'claude -p "审查 {} 并输出问题" > {}.review'
```

**实战案例：日志分析流水线**：

```bash
#!/bin/bash
# log-analyzer-pipeline.sh

LOG_FILE="$1"
OUTPUT_DIR="analysis_$(date +%Y%m%d_%H%M%S)"
mkdir -p "$OUTPUT_DIR"

echo "=== 日志分析流水线 ==="

# 步骤1：提取错误
echo "1. 提取错误日志..."
grep -E "ERROR|FATAL|Exception" "$LOG_FILE" > "$OUTPUT_DIR/errors.log"

# 步骤2：AI分类错误
echo "2. AI分类错误..."
cat "$OUTPUT_DIR/errors.log" | \
    claude -p "将这些错误按类型分类（网络错误、数据库错误、应用错误等）" \
    > "$OUTPUT_DIR/categorized.md"

# 步骤3：统计分析
echo "3. 统计分析..."
cat "$OUTPUT_DIR/categorized.md" | \
    claude -p "生成错误统计表格和趋势分析" \
    > "$OUTPUT_DIR/statistics.md"

# 步骤4：生成修复建议
echo "4. 生成修复建议..."
cat "$OUTPUT_DIR/categorized.md" | \
    claude -p "为每类错误提供具体的修复建议和预防措施" \
    > "$OUTPUT_DIR/recommendations.md"

# 步骤5：综合报告
echo "5. 生成综合报告..."
cat "$OUTPUT_DIR"/*.md | \
    claude -p "将以上内容整合成一份专业的日志分析报告" \
    > "$OUTPUT_DIR/final_report.md"

echo "=== 分析完成！报告位于: $OUTPUT_DIR/final_report.md ==="
```

**数据转换管道**：

```bash
# CSV转JSON
cat data.csv | claude -p "将这个CSV转换为JSON格式"

# JSON转Markdown表格
cat api_response.json | claude -p "将这个JSON转换为Markdown表格"

# 代码格式转换
cat legacy_code.js | claude -p "将这段ES5代码转换为现代ES6+语法"

# 多格式转换链
curl -s "https://api.example.com/data" | \
    claude -p "提取关键字段并转为CSV" | \
    claude -p "基于这个CSV生成数据可视化建议"
```

---

## 第二部分：Rewind/Checkpoint功能（2,500字）

Rewind/Checkpoint是Claude Code 2.0引入的革命性功能，让你可以大胆实验而无需担心破坏代码。

---

### 2.1 功能概述

Checkpoint（检查点）系统会在每次代码变更前自动保存状态，Rewind（回退）功能让你可以瞬间回到任何历史状态。

**核心价值**：

1. **安全实验**：大胆尝试重构，随时可以回退
2. **上下文清理**：不仅回退代码，还能清理"被污染"的对话上下文
3. **比Git更强**：Git只管代码，Checkpoint同时管理代码和AI对话状态
4. **零配置**：自动工作，无需手动设置

**工作原理**：

```
时间轴:
T1 ──────── T2 ──────── T3 ──────── T4 ──────── T5 (当前)
 │          │           │           │           │
[原始代码] [修改A]     [修改B]     [修改C]     [修改D]
[对话0]    [对话1]     [对话2]     [对话3]     [对话4]
 │          │           │           │           │
 └──────────┴───────────┴───────────┴───────────┘
                        可回退到任意检查点
```

---

### 2.2 激活Rewind功能

**方法一：双击Escape键（推荐）**

```bash
# 在交互模式中，快速按两次Escape键
You: [正在输入...然后发现需要回退]

[按 Esc + Esc]

╭─────────────────────────────────────────╮
│            Rewind Menu                   │
├─────────────────────────────────────────┤
│  Select a checkpoint to rewind to:       │
│                                          │
│  [1] 10:30:15 - Initial state           │
│  [2] 10:32:45 - Added auth module        │
│  [3] 10:35:20 - Modified database.py     │
│  [4] 10:38:10 - Created new tests        │
│  [5] 10:40:30 - Refactored API (current) │
│                                          │
│  [c] Cancel                              │
╰─────────────────────────────────────────╯

Select checkpoint (1-5 or c):
```

**方法二：使用/rewind命令**

```bash
You: /rewind

# 显示检查点列表
Available checkpoints:
1. [10:30:15] Initial state
   Files: (none modified)

2. [10:32:45] Added auth module
   Files: +src/auth.py, +src/auth_test.py

3. [10:35:20] Modified database.py
   Files: ~src/database.py

4. [10:38:10] Created new tests
   Files: +tests/test_auth.py, +tests/test_db.py

5. [10:40:30] Refactored API (current)
   Files: ~src/api.py, ~src/routes.py, -src/old_api.py

Enter checkpoint number to rewind (or 'cancel'):
```

---

### 2.3 三种恢复选项

Rewind提供三种恢复策略，满足不同场景需求：

**选项1：仅恢复对话（Conversation Only）**

```bash
Rewind Options:
[1] Conversation only - Keep code changes, reset AI context
[2] Code only - Keep conversation, revert file changes
[3] Both - Restore both code and conversation

Select option: 1

✓ Conversation rewound to checkpoint 3
✓ Code changes preserved
✓ AI context refreshed

# 场景：AI理解出现偏差，但代码修改是正确的
```

**选项2：仅恢复代码（Code Only）**

```bash
Select option: 2

Reverting files:
  ↩ src/api.py (restored)
  ↩ src/routes.py (restored)
  + src/old_api.py (restored deleted file)

✓ Code reverted to checkpoint 3
✓ Conversation history preserved

# 场景：代码改坏了，但对话中有有价值的分析
```

**选项3：同时恢复（Both）**

```bash
Select option: 3

Restoring checkpoint 3...
  ↩ Reverting 3 file changes
  ↩ Clearing 2 conversation turns

✓ Full state restored to checkpoint 3

# 场景：完全重来，回到某个已知良好的状态
```

---

### 2.4 自动检查点机制

Claude Code会在以下情况自动创建检查点：

**自动触发条件**：

| 触发条件 | 说明 |
|---------|------|
| 文件修改前 | 每次Write/Edit工具调用前 |
| 批量操作前 | 涉及多文件的操作前 |
| 危险命令前 | 执行rm、git reset等命令前 |
| 会话开始时 | 新会话自动创建初始检查点 |
| 用户手动请求 | /checkpoint命令 |

**检查点存储**：

```bash
# 检查点存储位置
~/.claude/checkpoints/
├── project_abc123/
│   ├── checkpoint_001.json    # 检查点元数据
│   ├── checkpoint_001/        # 文件快照
│   │   ├── src/
│   │   └── config/
│   ├── checkpoint_002.json
│   └── checkpoint_002/
└── project_def456/
    └── ...

# 检查点元数据结构
{
  "id": "checkpoint_001",
  "timestamp": "2025-12-11T10:30:15Z",
  "description": "Before adding auth module",
  "conversation_turn": 5,
  "files_snapshot": ["src/app.py", "config/settings.py"],
  "git_commit": "abc123" // 如果在git仓库中
}
```

---

### 2.5 手动检查点管理

**创建手动检查点**：

```bash
# 创建带描述的检查点
You: /checkpoint "完成用户认证模块"

✓ Checkpoint created: checkpoint_005
  Description: 完成用户认证模块
  Files captured: 5
  Conversation turn: 12

# 在重要操作前创建
You: 我要开始重构整个API层了
Claude: 好的，这是一个重大变更。让我先创建一个检查点。

Creating checkpoint before major refactoring...
✓ Checkpoint created: checkpoint_006
  Description: Pre-refactoring state

现在我开始重构API...
```

**查看检查点历史**：

```bash
You: /checkpoints

Checkpoint History (last 10):
─────────────────────────────────────────────────────────
ID          Time        Description                 Files
─────────────────────────────────────────────────────────
CP-010      10:45:30    Current state               -
CP-009      10:42:15    Added unit tests            +3
CP-008      10:38:20    Refactored database layer   ~2
CP-007      10:35:10    Fixed authentication bug    ~1
CP-006      10:30:05    Pre-refactoring state       -
CP-005      10:25:00    完成用户认证模块           +5
...
─────────────────────────────────────────────────────────

Commands:
  /rewind <id>     - Rewind to checkpoint
  /checkpoint-info <id> - View checkpoint details
  /checkpoint-delete <id> - Delete checkpoint
```

**清理检查点**：

```bash
# 删除特定检查点
You: /checkpoint-delete CP-003

Are you sure you want to delete checkpoint CP-003? (y/n): y
✓ Checkpoint CP-003 deleted

# 清理所有检查点（谨慎使用）
You: /checkpoints-clear

⚠ Warning: This will delete ALL checkpoints for this project.
This action cannot be undone.
Type 'DELETE ALL' to confirm: DELETE ALL

✓ All checkpoints cleared (8 checkpoints deleted)
```

---

### 2.6 Rewind最佳实践

**实践1：重大变更前创建检查点**

```bash
You: 我需要将整个项目从JavaScript迁移到TypeScript

Claude: 这是一个重大的项目变更。我会：
1. 首先创建一个检查点
2. 制定迁移计划
3. 分步骤执行迁移

/checkpoint "Pre-TypeScript migration"
✓ Checkpoint created

现在让我分析项目结构并制定迁移计划...
```

**实践2：实验性功能开发**

```bash
You: 我想尝试三种不同的缓存实现方案

Claude: 好的，我会为每种方案创建检查点，方便比较：

# 方案1
/checkpoint "Baseline - no cache"
[实现方案1: Redis缓存]
/checkpoint "Option 1: Redis cache"

# 回退测试方案2
/rewind "Baseline - no cache"
[实现方案2: Memcached]
/checkpoint "Option 2: Memcached"

# 回退测试方案3
/rewind "Baseline - no cache"
[实现方案3: 本地LRU缓存]
/checkpoint "Option 3: Local LRU"

现在你可以：
- /rewind "Option 1" - 使用Redis方案
- /rewind "Option 2" - 使用Memcached方案
- /rewind "Option 3" - 使用本地缓存方案
```

**实践3：清理被污染的上下文**

```bash
# 场景：AI对问题的理解出现偏差
You: [一系列基于错误理解的对话]

# 发现问题后
You: [按 Esc + Esc]

# 选择"仅恢复对话"
Select option: 1 (Conversation only)

✓ AI context refreshed

# 重新清晰地描述需求
You: 让我重新说明需求。我需要的是...
```

**实践4：与Git配合使用**

```bash
# Checkpoint捕获的是工作目录状态
# 可以与Git提交配合

You: 我已经完成了功能开发，准备提交

Claude: 好的。我建议的流程是：
1. 当前已有checkpoint保护工作状态
2. 运行测试确认一切正常
3. 创建Git提交
4. 如果后续发现问题，可以用Rewind回到提交前状态

Running tests...
✓ All tests passed

Creating git commit...
git add -A
git commit -m "feat: add user authentication module"
✓ Committed: abc123

# 即使已经commit，checkpoint仍然可用于回退工作目录
```

---

## 第三部分：Thinking Mode思考模式（2,000字）

Thinking Mode让Claude在回答前进行深度思考，对于复杂问题能显著提升回答质量。

---

### 3.1 思考模式基础

**什么是Thinking Mode**：

Thinking Mode是一种扩展推理模式，Claude会在给出最终答案前，先进行结构化的思考过程。这个思考过程会消耗额外的Token，但能显著提升复杂问题的解答质量。

**启用方式**：

```bash
# 方式1：使用/think命令（推荐）
You: /think 如何优化这个数据库查询？

# 方式2：在问题中加入关键词
You: think about how to optimize this query
You: think step by step about this algorithm

# 方式3：使用TAB键切换（Claude Code V2+）
You: [按 TAB 键切换思考模式开关]
Thinking Mode: ON ●
```

**思考过程展示**：

```bash
You: /think 分析这段递归代码的时间复杂度

Claude:
─────── Thinking ───────
让我分析这段递归代码：

1. 首先识别递归模式：
   - 函数 fib(n) 调用 fib(n-1) 和 fib(n-2)
   - 基准情况是 n <= 1

2. 构建递归树：
   - 每次调用产生2个子调用
   - 树的深度是 n
   - 所以总调用次数约为 2^n

3. 精确分析：
   - T(n) = T(n-1) + T(n-2) + O(1)
   - 这正好是斐波那契递推
   - 所以 T(n) ≈ O(φ^n)，其中φ是黄金比例

4. 空间复杂度：
   - 递归栈深度为 n
   - 所以空间复杂度是 O(n)
────────────────────────

**分析结果**：

这段递归斐波那契代码的复杂度是：
- **时间复杂度**: O(2^n)（更精确地说是O(φ^n) ≈ O(1.618^n)）
- **空间复杂度**: O(n)

**优化建议**：
1. 使用动态规划降到O(n)时间
2. 使用记忆化递归...
```

---

### 3.2 四个思考级别

Claude Code提供四个递进的思考深度级别：

**级别1：/think - 基础思考**

```bash
You: /think 这个函数有什么问题？

# Token预算：约4,000 tokens
# 响应时间：中等
# 适用场景：
# - 常规代码审查
# - 简单bug定位
# - 基础设计决策
```

**级别2：/think hard - 深入思考**

```bash
You: /think hard 如何设计一个高可用的消息队列系统？

# Token预算：约8,000 tokens
# 响应时间：较慢
# 适用场景：
# - 复杂系统设计
# - 性能优化分析
# - 多方案比较评估
```

**级别3：/think harder - 更深入思考**

```bash
You: /think harder 评估三种微服务架构方案的优劣

# Token预算：约16,000 tokens
# 响应时间：慢
# 适用场景：
# - 架构级别决策
# - 复杂算法设计
# - 全面的技术评估
```

**级别4：/ultrathink - 极限思考**

```bash
You: /ultrathink 设计一个支持百万并发的实时消息系统

# Token预算：约31,999 tokens（最大）
# 响应时间：很慢
# 适用场景：
# - 关键架构决策
# - 复杂系统完整设计
# - 需要穷尽所有可能性的分析
```

**级别对比表**：

| 级别 | 命令 | Token预算 | 适用复杂度 | 典型响应时间 |
|------|------|----------|-----------|-------------|
| L1 | `/think` | ~4K | 简单 | 5-15秒 |
| L2 | `/think hard` | ~8K | 中等 | 15-30秒 |
| L3 | `/think harder` | ~16K | 复杂 | 30-60秒 |
| L4 | `/ultrathink` | ~32K | 极复杂 | 1-3分钟 |

---

### 3.3 使用场景对比

**场景1：Bug修复**

```bash
# 简单bug - 使用/think
You: /think 这个空指针异常是怎么产生的？

# 复杂bug - 使用/think hard
You: /think hard 这个内存泄漏的根因是什么？

# 隐蔽bug - 使用/think harder
You: /think harder 这个偶发的竞态条件如何定位？
```

**场景2：代码优化**

```bash
# 单函数优化 - /think
You: /think 如何优化这个排序函数？

# 模块级优化 - /think hard
You: /think hard 如何优化这个数据处理模块的性能？

# 系统级优化 - /ultrathink
You: /ultrathink 如何将系统QPS从1万提升到10万？
```

**场景3：架构设计**

```bash
# 组件设计 - /think
You: /think 这个缓存服务应该如何设计接口？

# 模块设计 - /think hard
You: /think hard 如何设计这个支付模块的架构？

# 系统设计 - /ultrathink
You: /ultrathink 设计一个完整的电商系统架构
```

---

### 3.4 思考模式最佳实践

**实践1：渐进式升级**

```bash
# 先用基础思考快速探索
You: /think 这段代码有性能问题吗？

Claude: 初步分析显示可能有N+1查询问题...

# 发现复杂问题后升级
You: /think hard 详细分析这个N+1问题并给出所有解决方案

Claude: [更深入的分析]...

# 如果需要完整方案
You: /ultrathink 给出完整的数据库查询优化方案，包括索引设计
```

**实践2：明确问题范围**

```bash
# 不好的用法 - 问题太宽泛
You: /ultrathink 如何让代码更好？

# 好的用法 - 问题聚焦
You: /ultrathink 分析当前订单处理模块的以下方面：
1. 并发处理能力
2. 错误恢复机制
3. 与库存服务的交互
4. 数据一致性保证
给出完整的优化方案
```

**实践3：保存思考结果**

```bash
# 重要决策的思考过程值得保存
You: /ultrathink 评估将单体应用拆分为微服务的方案

Claude: [详细的分析过程]...

You: 将这个分析保存为文档

Claude:
Creating docs/architecture/microservices-evaluation.md...
✓ Analysis saved

# 日后可以参考这份决策文档
```

**实践4：与Checkpoint配合**

```bash
# 重大决策前创建检查点
You: /checkpoint "Before architecture decision"

# 使用ultrathink进行深度分析
You: /ultrathink 评估重构方案

# 实施后如果发现问题
You: /rewind "Before architecture decision"
```

---

### 3.5 Thinking Mode V2变化

Claude Code V2对思考模式进行了重要更新：

**V1 vs V2对比**：

| 特性 | V1 | V2 |
|------|----|----|
| 启用方式 | 仅通过/think命令 | TAB键切换 + /think命令 |
| 级别关键词 | 全部支持 | 保留ultrathink，其他改为开关 |
| 思考过程显示 | 可选显示 | 默认隐藏，可展开 |
| 状态指示 | 无 | 界面显示ON/OFF状态 |

**V2使用方式**：

```bash
# TAB键切换
You: [按TAB]
Thinking Mode: ON ●    # 状态指示

You: 分析这段代码
# Claude会自动使用思考模式

# 使用ultrathink获得最大思考预算
You: /ultrathink 这个问题需要最深入的分析

# 查看/隐藏思考过程
You: [点击 "Show thinking" 按钮]
```

---

## 第四部分：会话管理（2,500字）

高效的会话管理是提升开发效率的关键。本节详解所有会话相关功能。

---

### 4.1 /clear - 清空会话

**功能说明**：

完全清空当前对话历史，但保留CLAUDE.md配置。适合切换到完全不同的任务。

**使用方式**：

```bash
You: /clear

Conversation cleared.
─────────────────────────────
✓ Message history: Cleared
✓ Tool results: Cleared
✓ CLAUDE.md: Retained
✓ Project context: Retained
─────────────────────────────

You: [全新的对话开始]
```

**与/compact的区别**：

| 特性 | /clear | /compact |
|------|--------|----------|
| 对话历史 | 完全删除 | 压缩保留 |
| 关键信息 | 丢失 | 保留摘要 |
| Token释放 | 100% | 50-70% |
| 上下文连续性 | 断裂 | 保持 |
| 适用场景 | 换任务 | 继续同任务 |

**最佳使用时机**：

```bash
# ✅ 适合使用/clear的场景
- 完成一个任务，开始全新任务
- 对话走偏，需要完全重来
- 切换到不相关的项目或主题

# ❌ 不适合使用/clear的场景
- 正在解决一个问题的中途
- 需要AI记住之前的讨论
- 多步骤任务进行中
```

---

### 4.2 /compact - 压缩会话

**功能说明**：

智能压缩对话历史，保留关键信息摘要。适合长对话继续时释放Token空间。

**使用方式**：

```bash
You: /compact

Compacting conversation...
─────────────────────────────
Before: 145,000 tokens (72% of limit)
After:  52,000 tokens (26% of limit)
Saved:  93,000 tokens (64% reduction)
─────────────────────────────

Summary of preserved context:
• Working on: User authentication module
• Completed: Login API, session management
• In progress: Password reset feature
• Key decisions: Using JWT tokens, Redis for sessions
• Files modified: src/auth.py, src/routes.py

You: [继续之前的工作，AI保持上下文理解]
```

**自动Compact**：

```bash
# Claude Code在以下情况自动执行compact
Context usage: 95% ██████████████████░░ 190K/200K tokens

⚠ Context nearly full. Auto-compacting...
✓ Compacted: 190K → 68K tokens

# 可以禁用自动compact
You: /config set autoCompact false
```

**压缩策略**：

```bash
# 压缩保留的内容
✓ 当前任务目标
✓ 已完成的工作摘要
✓ 重要的代码变更
✓ 关键决策点
✓ 未解决的问题

# 压缩删除的内容
✗ 详细的中间对话
✗ 冗余的确认信息
✗ 过时的错误信息
✗ 重复的代码展示
```

---

### 4.3 /resume - 恢复会话

**功能说明**：

恢复之前的会话，支持选择历史会话或指定会话ID。

**使用方式**：

```bash
# 方式1：命令行参数恢复最近会话
$ claude --continue
# 或
$ claude -c

# 方式2：命令行参数恢复指定会话
$ claude --resume abc123
# 或
$ claude -r abc123

# 方式3：交互模式中恢复
You: /resume

Recent Sessions:
─────────────────────────────────────────────────────
ID          Date        Project           Summary
─────────────────────────────────────────────────────
ses_abc123  Today 10:30 my-app           Auth module
ses_def456  Today 09:15 my-app           Bug fixes
ses_ghi789  Yesterday   other-project    API design
ses_jkl012  2 days ago  my-app           Database schema
─────────────────────────────────────────────────────

Enter session ID (or number 1-4):
```

**会话恢复内容**：

```bash
Resuming session ses_abc123...
─────────────────────────────
✓ Conversation history: Restored (45 messages)
✓ Working directory: /Users/you/my-app
✓ Modified files context: 3 files
✓ Checkpoints: 5 available
─────────────────────────────

Last activity summary:
You were working on the authentication module.
Last message: "Next, let's add password validation"

You: [继续之前的工作]
```

---

### 4.4 /export - 导出会话

**功能说明**：

将当前会话导出为文件，支持多种格式，便于分享和存档。

**使用方式**：

```bash
# 默认导出（Markdown格式）
You: /export

Exporting conversation...
✓ Saved to: claude_session_2025-12-11_103045.md

# 指定格式导出
You: /export --format json
✓ Saved to: claude_session_2025-12-11_103045.json

You: /export --format html
✓ Saved to: claude_session_2025-12-11_103045.html

# 指定路径
You: /export ~/backups/important_session.md
✓ Saved to: /Users/you/backups/important_session.md
```

**导出格式说明**：

```bash
# Markdown格式（默认）- 适合阅读和文档
# Session Export: 2025-12-11 10:30:45
## Project: my-app
## Summary
Working on authentication module...

### Conversation

**You** (10:15:30):
Help me implement login API

**Claude** (10:15:45):
I'll help you create a secure login API...
[code blocks and explanations]

---

# JSON格式 - 适合程序处理
{
  "session_id": "ses_abc123",
  "project": "my-app",
  "timestamp": "2025-12-11T10:30:45Z",
  "messages": [
    {
      "role": "user",
      "content": "Help me implement login API",
      "timestamp": "2025-12-11T10:15:30Z"
    },
    ...
  ],
  "files_modified": ["src/auth.py"],
  "checkpoints": [...]
}

# HTML格式 - 适合分享和网页展示
<!DOCTYPE html>
<html>
<head><title>Claude Session Export</title></head>
<body>
  <h1>Session: my-app authentication</h1>
  ...
</body>
</html>
```

---

### 4.5 /sessions - 会话列表

**功能说明**：

查看和管理所有历史会话。

**使用方式**：

```bash
You: /sessions

Session Manager
═══════════════════════════════════════════════════════════════
Filter: All | Today | This Week | This Month | [Project: all ▼]
Sort: Recent ▼
═══════════════════════════════════════════════════════════════

Sessions (showing 1-10 of 45):
─────────────────────────────────────────────────────────────────
│ ID          │ Project      │ Date       │ Messages │ Summary    │
├─────────────┼──────────────┼────────────┼──────────┼────────────┤
│ ses_abc123  │ my-app       │ Today 10:30│ 45       │ Auth module│
│ ses_def456  │ my-app       │ Today 09:15│ 23       │ Bug fixes  │
│ ses_ghi789  │ other-proj   │ Yesterday  │ 67       │ API design │
│ ses_jkl012  │ my-app       │ 2 days ago │ 34       │ DB schema  │
│ ...         │              │            │          │            │
─────────────────────────────────────────────────────────────────

Commands:
  [r]esume <id>  - Resume session
  [e]xport <id>  - Export session
  [d]elete <id>  - Delete session
  [s]earch       - Search sessions
  [q]uit         - Exit session manager

Enter command:
```

**会话搜索**：

```bash
You: /sessions search "authentication"

Search Results for "authentication":
─────────────────────────────────────────────────────────────────
1. ses_abc123 (Today) - "Auth module implementation"
   Matches: "authentication", "login API", "JWT tokens"

2. ses_xyz789 (Last week) - "OAuth integration"
   Matches: "authentication provider", "Google OAuth"

3. ses_old001 (Last month) - "Security audit"
   Matches: "authentication bypass", "vulnerability"
─────────────────────────────────────────────────────────────────

Enter session ID to resume (or 'back'):
```

**会话清理**：

```bash
# 删除单个会话
You: /sessions delete ses_old001

Delete session ses_old001? (y/n): y
✓ Session deleted

# 批量清理旧会话
You: /sessions cleanup --older-than 30d

Found 23 sessions older than 30 days.
Delete these sessions? (y/n): y
✓ 23 sessions deleted

# 按项目清理
You: /sessions cleanup --project old-project --all

Delete all 15 sessions for project "old-project"? (y/n): y
✓ 15 sessions deleted
```

---

### 4.6 会话管理最佳实践

**实践1：建立命名规范**

```bash
# 使用/save创建有意义的会话名称
You: /save auth-module-v2

# 命名建议：
# [项目]-[功能]-[版本/日期]
# my-app-login-api
# my-app-bug-fix-20251211
# my-app-refactor-database
```

**实践2：定期清理**

```bash
# 每周清理过期会话
# 可以设置cron任务或手动执行
You: /sessions cleanup --older-than 7d --auto-confirm

# 保留重要会话
You: /sessions star ses_abc123  # 标记重要会话
You: /sessions cleanup --keep-starred
```

**实践3：利用会话进行知识传承**

```bash
# 完成重要功能后导出会话
You: /export ~/docs/sessions/auth-implementation.md

# 在README中记录
# ## 开发记录
# - 认证模块实现：docs/sessions/auth-implementation.md
# - 数据库设计：docs/sessions/database-design.md

# 新成员可以通过阅读会话了解决策过程
```

**实践4：多会话并行工作**

```bash
# 终端1：主功能开发
$ claude -c  # 继续最近的会话

# 终端2：Bug修复（新会话）
$ claude
You: 专注修复issue #123

# 终端3：恢复特定会话
$ claude -r ses_design123
You: 继续讨论架构设计

# 各会话独立，互不干扰
```

---

## 第五部分：CLAUDE.md配置层级（2,000字）

CLAUDE.md是Claude Code的灵魂配置文件，理解其层级系统对于团队协作至关重要。

---

### 5.1 配置层级概述

Claude Code支持多层级的CLAUDE.md配置，采用"就近优先"原则合并。

**层级结构**：

```
优先级（从高到低）：
═══════════════════════════════════════════════════════════
1. [企业级] Enterprise Policy (最高优先级)
   位置：由管理员配置，不可覆盖的安全策略

2. [项目级] Project CLAUDE.md
   位置：./CLAUDE.md（项目根目录）
   作用：团队共享的项目规范

3. [模块级] Module Rules
   位置：.claude/rules/*.md 或子目录CLAUDE.md
   作用：特定模块的规则

4. [用户级] User CLAUDE.md
   位置：~/.claude/CLAUDE.md
   作用：个人偏好设置
═══════════════════════════════════════════════════════════
```

**合并示例**：

```
~/.claude/CLAUDE.md（用户级）
├── 偏好：使用中文回复
├── 风格：简洁直接
└── 工具：允许所有

./CLAUDE.md（项目级）
├── 语言：TypeScript
├── 风格：详细注释
└── 规范：Google风格

./src/api/CLAUDE.md（模块级）
├── 特殊：REST API规范
└── 测试：每个端点必须有测试

合并结果（在./src/api/目录工作时）：
├── 偏好：使用中文回复（用户级）
├── 风格：详细注释（项目级覆盖用户级）
├── 语言：TypeScript（项目级）
├── 规范：Google风格（项目级）
├── 特殊：REST API规范（模块级追加）
└── 测试：每个端点必须有测试（模块级追加）
```

---

### 5.2 用户级配置：~/.claude/CLAUDE.md

**位置**：`~/.claude/CLAUDE.md`（所有项目共享）

**适合放置的内容**：

```markdown
# 个人Claude Code配置

## 语言偏好
- 始终使用中文回复
- 代码注释使用英文

## 通用编码风格
- 优先使用函数式编程风格
- 变量命名使用camelCase
- 提交信息使用Conventional Commits格式

## 个人工具偏好
- 优先使用vim键位
- 生成代码时自动添加类型注解
- 错误处理使用try-catch而非.catch()

## 通用安全规则
- 永不在代码中硬编码密钥
- 始终验证用户输入
- 敏感操作需要确认
```

**不适合放置的内容**：

```markdown
# ❌ 以下内容不应放在用户级配置

# 项目特定的依赖
- 使用React 18
- 使用Prisma ORM

# 团队规范
- PR必须有两人review

# 项目架构
- 使用微服务架构
```

---

### 5.3 项目级配置：./CLAUDE.md

**位置**：项目根目录的`CLAUDE.md`

**推荐结构**：

```markdown
# ProjectName - Claude Code配置

## 项目概述
- **名称**：MyAwesomeApp
- **类型**：Web应用
- **状态**：开发中

## 技术栈
- 前端：React 18 + TypeScript
- 后端：FastAPI + Python 3.11
- 数据库：PostgreSQL 15
- 缓存：Redis 7

## 目录结构
```
src/
├── frontend/     # React前端
├── backend/      # FastAPI后端
├── shared/       # 共享类型
└── scripts/      # 工具脚本
```

## 编码规范
### 通用规则
- 所有代码必须有类型注解
- 每个公开函数必须有文档字符串
- 测试覆盖率不低于80%

### 前端规范
- 使用函数组件和Hooks
- 状态管理使用Zustand
- 样式使用Tailwind CSS

### 后端规范
- API遵循REST规范
- 使用Pydantic进行数据验证
- 错误响应遵循RFC 7807

## Git规范
- 分支命名：feature/xxx, bugfix/xxx, hotfix/xxx
- 提交格式：type(scope): description
- PR必须通过CI检查

## 重要文件
- API文档：docs/api.md
- 部署指南：docs/deployment.md
- 变更日志：CHANGELOG.md
```

**检入Git的建议**：

```bash
# CLAUDE.md应该检入版本控制
git add CLAUDE.md
git commit -m "docs: add Claude Code configuration"

# 团队成员将自动获得相同的AI助手配置
```

---

### 5.4 模块级配置：.claude/rules/

**位置**：`.claude/rules/*.md` 或子目录的`CLAUDE.md`

**场景1：按功能分类的规则**

```
.claude/
└── rules/
    ├── api-rules.md      # API相关规则
    ├── database-rules.md # 数据库相关规则
    ├── security-rules.md # 安全相关规则
    └── testing-rules.md  # 测试相关规则
```

```markdown
<!-- .claude/rules/api-rules.md -->
# API开发规则

## 端点命名
- 使用复数名词：/users, /posts, /comments
- 使用连字符：/user-profiles, /order-items

## 响应格式
- 成功：{ "data": ..., "meta": ... }
- 错误：{ "error": { "code": "...", "message": "..." } }

## HTTP状态码
- 200：成功
- 201：创建成功
- 400：请求错误
- 401：未认证
- 403：未授权
- 404：未找到
- 500：服务器错误

## 必需的中间件
- 认证：authMiddleware
- 限流：rateLimitMiddleware
- 日志：loggingMiddleware
```

**场景2：子目录专用配置**

```
src/
├── api/
│   ├── CLAUDE.md      # API模块专用配置
│   └── routes.py
├── models/
│   ├── CLAUDE.md      # 数据模型专用配置
│   └── user.py
└── services/
    ├── CLAUDE.md      # 服务层专用配置
    └── auth.py
```

---

### 5.5 @import语法

使用`@`语法可以导入其他Markdown文件，保持主配置文件简洁。

**基本用法**：

```markdown
# CLAUDE.md

## 项目概述
这是一个电商平台项目。

## 详细规范
@docs/coding-standards.md
@docs/api-guidelines.md
@docs/database-schema.md

## 团队信息
@docs/team-contacts.md
```

**导入解析**：

```
CLAUDE.md
├── 直接内容：项目概述
├── @docs/coding-standards.md → 导入编码规范
├── @docs/api-guidelines.md → 导入API指南
├── @docs/database-schema.md → 导入数据库架构
└── @docs/team-contacts.md → 导入团队信息

Claude读取时，所有导入的文件内容会被合并
```

**条件导入（高级）**：

```markdown
# CLAUDE.md

## 基础配置
@.claude/rules/base.md

## 环境特定配置
<!-- 根据工作目录自动加载 -->
@.claude/rules/frontend.md  <!-- 在frontend/目录下自动加载 -->
@.claude/rules/backend.md   <!-- 在backend/目录下自动加载 -->
```

**最佳实践**：

```markdown
# 推荐的导入组织方式

## 1. 规范类导入
@docs/standards/code-style.md
@docs/standards/naming-convention.md

## 2. 架构类导入
@docs/architecture/overview.md
@docs/architecture/data-flow.md

## 3. 上下文类导入
@docs/context/business-rules.md
@docs/context/domain-terms.md

## 4. 工作流类导入
@docs/workflow/development.md
@docs/workflow/deployment.md
```

---

### 5.6 优先级与合并规则

**冲突解决规则**：

```
规则：更具体的配置覆盖更通用的配置

示例：
~/.claude/CLAUDE.md:      "使用2空格缩进"
./CLAUDE.md:              "使用4空格缩进"
./src/legacy/CLAUDE.md:   "使用Tab缩进"

结果：
- 在./src/legacy/目录：使用Tab缩进
- 在./src/其他目录：使用4空格缩进
- 在其他项目：使用2空格缩进
```

**追加vs覆盖**：

```markdown
# 追加行为（列表类配置）
~/.claude/CLAUDE.md:
  忽略文件：node_modules, .git

./CLAUDE.md:
  忽略文件：dist, build

合并结果：
  忽略文件：node_modules, .git, dist, build  # 追加

# 覆盖行为（单值配置）
~/.claude/CLAUDE.md:
  首选语言：中文

./CLAUDE.md:
  首选语言：英文

合并结果：
  首选语言：英文  # 覆盖
```

---

## 第六部分：实战案例（500字）

将本课所学知识整合到实际项目工作流中。

---

### 6.1 完整工作流示例

**场景**：开发一个新的用户认证模块

```bash
# 1. 启动Claude Code
$ claude --project ~/my-app

# 2. 创建检查点
You: /checkpoint "开始认证模块开发"

# 3. 使用思考模式进行设计
You: /think hard 设计一个安全的JWT认证系统，考虑：
- Token刷新机制
- 多设备登录处理
- 安全存储方案

# 4. 开始实现（Claude自动创建检查点）
You: 开始实现登录API

# 5. 遇到复杂问题时升级思考模式
You: /ultrathink 如何处理Token被盗用的场景？

# 6. 保存重要进度
You: /save auth-module-progress

# 7. 如果走错方向，使用Rewind
You: [Esc + Esc] → 选择恢复到之前的检查点

# 8. 完成后压缩会话继续其他工作
You: /compact

# 9. 导出会话作为文档
You: /export ~/docs/auth-development.md
```

---

### 6.2 团队协作配置示例

**项目CLAUDE.md**：

```markdown
# E-commerce Platform - Claude Code配置

## 项目信息
- 仓库：github.com/company/ecommerce
- 团队：Platform Team

## 核心规范
@.claude/rules/code-style.md
@.claude/rules/api-design.md
@.claude/rules/database.md

## 工作约定
- PR需要关联Issue
- 提交信息遵循Conventional Commits
- 新功能需要添加测试

## 目录说明
- src/api/ - REST API端点
- src/services/ - 业务逻辑层
- src/models/ - 数据模型
- src/utils/ - 工具函数
```

---

## 本课总结

### 掌握的技能

通过本课学习，你现在能够：

1. **5种工作模式**：根据场景选择最合适的工作模式
2. **Rewind/Checkpoint**：大胆实验，安全回退
3. **Thinking Mode**：针对问题复杂度选择思考级别
4. **会话管理**：高效组织和复用对话历史
5. **CLAUDE.md配置**：建立团队一致的AI助手体验

### 下一步学习

完成本课后，建议继续学习：

1. **模块3《Commands系统》**：创建自定义命令
2. **模块4《MCP集成》**：扩展Claude Code能力
3. **模块5《Hooks系统》**：自动化工作流

---

**课程版本**：V1.0
**最后更新**：2025-12-11
**下一课**：模块3《Commands系统深度教程》

**恭喜完成交互模式深度教程！**

## 第2部分：工作流模式（3,000字）

### 2.1 REPL交互式工作流

**特点**：实时对话，适合探索性开发

#### 2.1.1 典型场景

**场景1：探索新代码库**
```bash
You: 这个项目的主入口在哪里？
Claude: [分析] 主入口是 src/main.py

You: main.py 做了什么？
Claude: [阅读文件] 它初始化了FastAPI应用，注册了路由...

You: 路由定义在哪里？
Claude: [检索] 路由定义在 src/api/routes.py
```

**场景2：迭代式重构**
```bash
You: 重构 process_order 函数
Claude: [完成重构]

You: 性能不够好，优化一下
Claude: [优化性能]

You: 再加上异常处理
Claude: [添加异常处理]
```

#### 2.1.2 REPL工作流最佳实践

**DO（推荐做法）**：
- ✅ 小步快跑：一次完成一个小任务
- ✅ 及时验证：每步完成后测试
- ✅ 利用上下文：后续问题可以简短
- ✅ 使用Thinking Mode：复杂任务加`think hard`

**DON'T（避免做法）**：
- ❌ 一次性描述太复杂的需求
- ❌ 长时间不清理上下文
- ❌ 不验证就继续下一步
- ❌ 频繁切换不相关的任务

### 2.2 Headless自动化工作流

**特点**：无交互，适合自动化任务

#### 2.2.1 Headless模式基础

```bash
# 单次执行
claude -p "列出所有TODO注释"

# 管道模式
echo "优化这段代码" | claude

# 文件输入
claude < task.txt

# 输出到文件
claude -p "生成测试用例" > tests.py
```

#### 2.2.2 实战案例：自动化重构

**需求**：批量重构100个文件的错误处理

**方案1：Shell脚本**
```bash
#!/bin/bash
# refactor.sh

FILES=$(find src/ -name "*.py")

for file in $FILES; do
  echo "重构 $file 的错误处理" | claude -p "
    文件：$file
    任务：将所有 try-except 改为使用自定义异常类
  "
done
```

**方案2：Python脚本**
```python
# refactor.py
import subprocess
from pathlib import Path

def refactor_file(file_path):
    prompt = f"""
    文件：{file_path}
    任务：重构错误处理
    1. 使用自定义异常类
    2. 添加详细的错误日志
    3. 保持原有逻辑不变
    """

    result = subprocess.run(
        ["claude", "-p", prompt],
        capture_output=True,
        text=True
    )

    return result.stdout

# 批量处理
for file in Path("src").rglob("*.py"):
    print(f"处理 {file}")
    refactor_file(file)
```

#### 2.2.3 Headless高级技巧

**技巧1：结果验证**
```bash
#!/bin/bash
# 带验证的自动化

claude -p "重构 user.py" && \
pytest tests/test_user.py && \
echo "✅ 重构成功" || \
echo "❌ 重构失败，回滚"
```

**技巧2：批量任务并行**
```bash
# parallel_refactor.sh
find src/ -name "*.py" | \
  parallel -j 4 'claude -p "优化{}"'
```

**技巧3：结果聚合**
```bash
# 生成报告
claude -p "分析所有Python文件的复杂度" | \
  tee complexity_report.txt | \
  grep "高复杂度" > high_complexity_files.txt
```

### 2.3 SDK集成工作流

**特点**：程序化调用，适合工具集成

#### 2.3.1 Node.js SDK示例

```javascript
// claude-tool.js
import { ClaudeCode } from '@anthropic/claude-code';

const claude = new ClaudeCode({
  apiKey: process.env.ANTHROPIC_API_KEY
});

// 单次任务
async function refactorFile(filePath) {
  const result = await claude.run({
    prompt: `重构 ${filePath} 的错误处理`,
    workingDir: process.cwd(),
    tools: ['Edit', 'Read', 'Bash']
  });

  return result;
}

// 交互式会话
async function interactiveSession() {
  const session = await claude.createSession();

  // 第一轮
  await session.send("列出所有TODO");

  // 第二轮（保持上下文）
  await session.send("把第一个TODO实现了");

  // 第三轮
  await session.send("运行测试验证");

  await session.close();
}
```

#### 2.3.2 Python SDK示例

```python
# claude_tool.py
from anthropic import ClaudeCode

claude = ClaudeCode(api_key=os.getenv("ANTHROPIC_API_KEY"))

# 单次任务
def refactor_file(file_path: str) -> str:
    result = claude.run(
        prompt=f"重构 {file_path} 的错误处理",
        working_dir=os.getcwd(),
        tools=["Edit", "Read", "Bash"]
    )
    return result.output

# 交互式会话
def interactive_session():
    with claude.session() as session:
        # 保持上下文的多轮对话
        session.send("列出所有TODO")
        session.send("把第一个TODO实现了")
        session.send("运行测试验证")
```

#### 2.3.3 SDK集成实战：CI/CD集成

```yaml
# .github/workflows/ai-review.yml
name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  ai-review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Claude Code
        run: npm install -g @anthropic/claude-code

      - name: AI Code Review
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          claude -p "
            分析此PR的代码变更：
            1. 检查潜在的bug
            2. 评估性能影响
            3. 检查安全问题
            4. 提出改进建议
          " > review.md

      - name: Post Review
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            const review = fs.readFileSync('review.md', 'utf8');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: review
            });
```

### 2.4 管道式工作流

**特点**：链式处理，适合多步骤任务

#### 2.4.1 管道模式基础

```bash
# 单向管道
echo "优化性能" | claude | tee result.txt

# 多阶段管道
cat task.txt | \
  claude -p "第一步：分析问题" | \
  claude -p "第二步：生成方案" | \
  claude -p "第三步：实施方案"
```

#### 2.4.2 实战：文档自动生成管道

```bash
#!/bin/bash
# doc_pipeline.sh

# 阶段1：提取代码结构
claude -p "分析项目结构" > structure.md

# 阶段2：生成API文档
claude -p "
  基于项目结构生成API文档
  输入：structure.md
" > api_docs.md

# 阶段3：生成用户手册
claude -p "
  基于API文档生成用户手册
  输入：api_docs.md
  输出格式：Markdown
" > user_guide.md

# 阶段4：生成PDF
pandoc user_guide.md -o user_guide.pdf
```

#### 2.4.3 复杂管道：代码质量检查流水线

```bash
#!/bin/bash
# quality_pipeline.sh

PROJECT_DIR=$1

# 1. 静态分析
echo "阶段1：静态分析"
claude -p "对 $PROJECT_DIR 执行静态代码分析" | \
  tee static_analysis.txt

# 2. 安全审计
echo "阶段2：安全审计"
claude -p "
  基于静态分析结果，检查安全漏洞
  输入：static_analysis.txt
" | tee security_audit.txt

# 3. 性能分析
echo "阶段3：性能分析"
claude -p "
  分析性能瓶颈
  输入：static_analysis.txt
" | tee performance_analysis.txt

# 4. 生成改进建议
echo "阶段4：生成改进建议"
claude -p "
  综合以上分析，生成改进方案
  输入：
  - static_analysis.txt
  - security_audit.txt
  - performance_analysis.txt
" > improvement_plan.md

echo "✅ 质量检查完成，报告已生成"
```

---

## 第3部分：进阶技巧（3,000字）

### 3.1 高效提示词技巧

#### 3.1.1 结构化提示词

**基础提示词**（效果一般）：
```bash
You: 重构这个函数
```

**结构化提示词**（效果更好）：
```bash
You: 重构 calculate_total 函数：

【目标】
- 提升性能（当前O(n²) → 期望O(n)）
- 提高可读性

【约束】
- 保持API不变
- 保持向后兼容
- 不引入新依赖

【测试】
- 运行 pytest tests/test_calculation.py
- 确保所有测试通过
```

#### 3.1.2 角色扮演技巧

**技巧1：专家模式**
```bash
You: 作为资深Python架构师，审查这段代码

Claude: [以架构师视角分析]
从架构层面看，这段代码有以下问题：
1. 紧耦合：业务逻辑和数据访问混在一起
2. 缺乏抽象：没有接口层
3. 难以测试：依赖具体实现而非接口
```

**技巧2：逐步引导**
```bash
You: 我要优化一个慢查询

Claude: 好的，让我们逐步分析：
1. 首先，查询慢的原因是什么？
2. 数据量有多大？
3. 索引情况如何？

You: 数据量100万，没有索引

Claude: 明白了，首要任务是添加索引...
```

#### 3.1.3 上下文窗口管理

**问题**：长时间对话后，早期信息可能被遗忘

**解决方案**：
```bash
# 技巧1：定期总结
You: 总结一下我们到目前为止做了什么

Claude: 总结如下：
1. 重构了用户认证模块
2. 优化了数据库查询
3. 添加了单元测试
当前进行到：添加API文档

# 技巧2：重要信息置顶
You: 记住：本项目使用Python 3.11，禁止使用3.12特性

# 技巧3：文件引用而非粘贴
You: /add src/core.py  # 引用文件
You: 优化 core.py 中的 process 函数
```

### 3.2 多项目工作流

#### 3.2.1 项目切换

```bash
# 方案1：多终端
# 终端1
cd project-a && claude

# 终端2
cd project-b && claude

# 方案2：会话导出/恢复
cd project-a
claude
You: /export project-a-session.json

cd project-b
claude
You: /export project-b-session.json

# 恢复会话
cd project-a
claude --resume project-a-session.json
```

#### 3.2.2 跨项目知识复用

**场景**：在项目A学到的技巧应用到项目B

```bash
# 项目A：学习最佳实践
cd project-a
claude
You: 分析这个项目的错误处理模式
Claude: [分析] 使用了Result类型模式...
You: /export error-handling-pattern.json

# 项目B：应用最佳实践
cd project-b
claude
You: 参考这个错误处理模式：error-handling-pattern.json
You: 重构我们的错误处理
```


### 3.3 团队协作模式

#### 3.3.1 共享提示词库

**团队提示词文件**：
```bash
# team-prompts.sh

# 代码审查模板
code_review() {
  claude -p "
    作为Senior Developer审查代码：
    1. 架构设计是否合理
    2. 是否遵循团队规范
    3. 潜在的性能问题
    4. 安全隐患
    文件：$1
  "
}

# 测试生成模板
generate_tests() {
  claude -p "
    为以下代码生成测试：
    - 覆盖率要求：>80%
    - 框架：pytest
    - 包含边界测试
    文件：$1
  "
}

# 使用
code_review src/api/user.py
generate_tests src/services/payment.py
```

#### 3.3.2 代码审查工作流

**场景**：PR审查自动化

```bash
#!/bin/bash
# pr-review.sh

PR_NUMBER=$1

# 1. 获取变更文件
changed_files=$(gh pr view $PR_NUMBER --json files -q '.files[].path')

# 2. 对每个文件进行AI审查
for file in $changed_files; do
  echo "审查 $file"
  claude -p "
    审查此PR的变更：
    文件：$file

    检查项：
    1. 代码质量
    2. 潜在bug
    3. 性能影响
    4. 安全问题
    5. 测试覆盖
  " >> review.md
done

# 3. 发布审查评论
gh pr comment $PR_NUMBER -F review.md
```

### 3.4 性能优化技巧

#### 3.4.1 减少Token消耗

**技巧1：使用文件引用**
```bash
# ❌ 低效：粘贴大段代码
You: [粘贴500行代码]
You: 优化这段代码

# ✅ 高效：引用文件
You: /add src/large_file.py
You: 优化 large_file.py 中的 process_data 函数
```

**技巧2：精确定位**
```bash
# ❌ 低效：让AI搜索
You: 找到性能瓶颈并优化

# ✅ 高效：精确定位
You: 优化 src/api/user.py 的 get_user_history 函数
     该函数在处理大量数据时很慢
```

**技巧3：分步执行**
```bash
# ❌ 低效：一次性大任务
You: 重构整个项目的错误处理

# ✅ 高效：分步执行
You: 先重构 src/api/ 下的错误处理
You: [完成后] 接下来重构 src/services/
```

#### 3.4.2 加速响应

**技巧1：预加载上下文**
```bash
# 在CLAUDE.md中预定义常用信息
# 避免每次对话都要说明

## 项目信息
- Python 3.11
- FastAPI + Pydantic
- PostgreSQL

## 编码规范
- 必须有类型注解
- 使用async/await
```

**技巧2：使用快捷命令**
```bash
# 创建alias
alias cr="claude -p 'code review:'"
alias test="claude -p 'generate tests for:'"
alias doc="claude -p 'generate documentation for:'"

# 使用
cr src/api/user.py
test src/services/payment.py
```

### 3.5 调试与问题排查

#### 3.5.1 调试模式

```bash
# 启用详细日志
claude --debug

# 查看工具调用详情
You: [任务]
Claude:
[DEBUG] Tool: Read
[DEBUG] Args: {"path": "src/main.py"}
[DEBUG] Result: [文件内容]
```

#### 3.5.2 常见问题排查

**问题1：AI理解错误**
```bash
# 症状：AI做了不相关的事情

# 解决：使用Rewind + 更明确的指令
You: <Escape键回退>
You: think 仔细理解需求：
     我要优化 calculate_total 函数的性能
     不是重构整个文件
```

**问题2：工具执行失败**
```bash
# 症状：Edit工具找不到代码

# 解决：使用Read工具先验证
You: 先用Read工具读取 src/main.py
You: 确认后再修改 process_order 函数
```

**问题3：上下文混乱**
```bash
# 症状：AI回复不一致

# 解决：清理上下文
You: /clear
You: [重新开始，明确描述需求]
```

---

## 第4部分：实战案例（2,500字）

### 4.1 案例1：大规模代码重构

**需求**：将一个单体应用拆分为微服务架构

#### 4.1.1 任务规划

```bash
You: think harder 我要将这个Django单体应用拆分为微服务

Claude: <thinking>
第一层：理解当前架构
- 单体Django应用
- 数据库：PostgreSQL（所有表在一起）
- 模块：用户、订单、支付、库存

第二层：拆分策略
- 服务划分：按业务边界
  - 用户服务
  - 订单服务
  - 支付服务
  - 库存服务

第三层：技术挑战
- 数据库拆分：外键关系处理
- 事务处理：分布式事务
- 服务通信：同步vs异步
- 数据一致性：最终一致性

第四层：实施方案
- 阶段1：识别服务边界
- 阶段2：提取用户服务（最独立）
- 阶段3：提取订单服务
- 阶段4：提取支付和库存服务
- 阶段5：渐进式切流
</thinking>

我建议采用绞杀者模式（Strangler Pattern），逐步迁移...
```

#### 4.1.2 实施步骤

**步骤1：分析依赖关系**
```bash
You: 分析 models.py 中各模型的依赖关系
Claude: [生成依赖图]
User -> Order (外键)
Order -> Payment (外键)
Order -> OrderItem -> Product -> Inventory
```

**步骤2：提取第一个服务**
```bash
You: 提取用户服务：
1. 创建独立的FastAPI项目
2. 迁移User模型和相关逻辑
3. 保持API兼容

Claude: [创建新项目结构]
user-service/
  src/
    api/
    models/
    services/
```

**步骤3：数据迁移**
```bash
You: 生成数据库迁移脚本：
- 从主数据库迁移users表到user-service数据库
- 保持数据同步（过渡期）

Claude: [生成迁移脚本]
```

**步骤4：验证与回滚**
```bash
You: 生成验证脚本，对比新旧服务的返回结果
Claude: [生成对比测试]

You: 如果验证失败，准备回滚方案
Claude: [准备回滚脚本]
```

### 4.2 案例2：性能优化

**需求**：优化一个慢查询，响应时间从5秒降到500ms以内

#### 4.2.1 问题诊断

```bash
You: think hard 分析这个查询为什么慢：

def get_user_order_history(user_id):
    orders = Order.objects.filter(user_id=user_id)
    result = []
    for order in orders:
        items = OrderItem.objects.filter(order_id=order.id)
        for item in items:
            product = Product.objects.get(id=item.product_id)
            result.append({
                'order': order,
                'item': item,
                'product': product
            })
    return result

Claude: <thinking>
性能问题分析：
1. N+1查询问题：
   - 查询1次orders（假设100条）
   - 每个order查询1次items（100次）
   - 每个item查询1次product（假设300次）
   - 总计：1 + 100 + 300 = 401次数据库查询！

2. 缺少索引：
   - OrderItem.order_id 可能没有索引
   - Product.id 是主键有索引

3. 数据量：
   - 如果用户有100个订单
   - 每个订单平均3个商品
   - 需要查询401次

优化策略：
1. 使用select_related和prefetch_related
2. 一次查询获取所有数据
3. 添加必要的索引
</thinking>

优化方案：
```

#### 4.2.2 优化实施

```python
# 优化版本
def get_user_order_history_optimized(user_id):
    orders = Order.objects.filter(
        user_id=user_id
    ).prefetch_related(
        Prefetch(
            'items',
            queryset=OrderItem.objects.select_related('product')
        )
    )

    result = []
    for order in orders:
        for item in order.items.all():
            result.append({
                'order': order,
                'item': item,
                'product': item.product
            })
    return result
```

**验证优化效果**：
```bash
You: 生成性能测试脚本，对比优化前后

Claude: [生成测试脚本]
import time

# 测试原版
start = time.time()
result_old = get_user_order_history(user_id)
time_old = time.time() - start

# 测试优化版
start = time.time()
result_new = get_user_order_history_optimized(user_id)
time_new = time.time() - start

print(f"原版耗时：{time_old:.2f}秒")
print(f"优化后：{time_new:.2f}秒")
print(f"提升：{time_old/time_new:.1f}x")
```

### 4.3 案例3：自动化测试生成

**需求**：为整个项目生成单元测试，覆盖率达到80%以上

#### 4.3.1 批量测试生成

```bash
#!/bin/bash
# generate_tests.sh

# 找到所有未测试的文件
untested_files=$(find src/ -name "*.py" | while read file; do
  test_file="tests/test_$(basename $file)"
  if [ ! -f "$test_file" ]; then
    echo $file
  fi
done)

# 为每个文件生成测试
for file in $untested_files; do
  echo "生成测试：$file"

  claude -p "
    为以下文件生成完整的单元测试：
    文件：$file

    要求：
    1. 使用pytest框架
    2. 测试所有公共函数
    3. 包含边界测试
    4. 包含异常测试
    5. 使用fixtures处理依赖
    6. 覆盖率目标：>90%

    输出文件：tests/test_$(basename $file)
  "
done

# 运行测试验证
pytest tests/ --cov=src/ --cov-report=html
```

#### 4.3.2 测试质量检查

```bash
You: 检查生成的测试质量

Claude: [运行测试并分析覆盖率报告]
Coverage Report:
- src/api/user.py: 92%
- src/api/order.py: 85%
- src/services/payment.py: 78% ⚠️ 未达标

建议补充测试：
1. payment.py 的异常处理分支未覆盖
2. 需要添加mock测试外部API调用
```

### 4.4 案例4：文档自动生成

**需求**：为整个API项目生成完整的文档

#### 4.4.1 多阶段生成

```bash
You: 分阶段生成文档：
1. 提取API路由和参数
2. 生成API文档（OpenAPI格式）
3. 生成用户手册（Markdown）
4. 生成部署指南

Claude: [执行阶段1]
✅ 已分析项目结构，找到23个API端点

You: 继续阶段2
Claude: [生成OpenAPI规范]
✅ 已生成 openapi.yaml

You: 继续阶段3
Claude: [生成用户手册]
✅ 已生成 user-guide.md

You: 继续阶段4
Claude: [生成部署指南]
✅ 已生成 deployment.md
```

#### 4.4.2 文档持续更新

**设置Git Hook**：
```bash
#!/bin/bash
# .git/hooks/pre-commit

# 检查代码变更
changed_files=$(git diff --cached --name-only --diff-filter=ACM | grep "src/api/")

if [ -n "$changed_files" ]; then
  echo "检测到API变更，更新文档..."

  # 重新生成API文档
  claude -p "更新API文档，基于变更：$changed_files"

  # 添加文档到提交
  git add docs/api.md
fi
```

---


## 第5部分：CLAUDE.md配置（1,500字）

### 5.1 CLAUDE.md三层架构

#### 5.1.1 层级优先级

```
用户级 CLAUDE.md (最高优先级)
  ↓ 覆盖
项目级 CLAUDE.md
  ↓ 覆盖
模块级 CLAUDE.md (最低优先级)
```

#### 5.1.2 用户级配置

**位置**：`~/.claude/CLAUDE.md`

```markdown
# 我的全局配置

## 编码习惯
- 我习惯使用4空格缩进
- 我喜欢详细的注释
- 我倾向于函数式编程风格

## 工具偏好
- Git：使用conventional commits格式
- 测试：优先使用pytest
- 文档：使用Google风格docstring

## 沟通偏好
- 先给出方案概述，再详细实施
- 复杂操作前先确认
- 使用中文交流
```

#### 5.1.3 项目级配置

**位置**：`项目根目录/CLAUDE.md`

```markdown
# 项目：电商系统

## 技术栈
- Python 3.11 + FastAPI
- PostgreSQL 14 + Redis 7
- Docker + Kubernetes

## 项目结构
src/
  api/        # API路由
  models/     # 数据模型（Pydantic）
  services/   # 业务逻辑
  db/         # 数据库访问
  utils/      # 工具函数
tests/
  unit/       # 单元测试
  integration/ # 集成测试

## 编码规范
- 所有函数必须有类型注解
- 使用Pydantic做数据验证
- 异步优先（async/await）
- 错误处理使用自定义异常类

## Git规范
- 分支命名：feature/xxx, bugfix/xxx
- 提交格式：feat/fix/docs/refactor: description
- PR必须通过CI检查
```

#### 5.1.4 模块级配置

**位置**：`src/api/CLAUDE.md`

```markdown
# API模块

## 模块职责
- 定义FastAPI路由
- 处理请求验证
- 调用服务层
- 返回标准化响应

## 路由规范
- RESTful风格
- 使用Pydantic模型做请求/响应验证
- 统一错误处理

## 示例
```python
@router.post("/users", response_model=UserResponse)
async def create_user(user: UserCreate):
    """创建用户"""
    return await user_service.create(user)
```
```

### 5.2 上下文自动注入

#### 5.2.1 工作原理

```bash
# 当你在项目目录启动Claude Code时
cd my-project
claude

# Claude自动加载：
# 1. ~/.claude/CLAUDE.md（用户级）
# 2. my-project/CLAUDE.md（项目级）
# 3. 如果在子目录，加载子目录的CLAUDE.md（模块级）
```

#### 5.2.2 动态上下文

**在CLAUDE.md中使用变量**：

```markdown
# 项目配置

## 环境变量
- `{{PROJECT_NAME}}` - 项目名称
- `{{PYTHON_VERSION}}` - Python版本
- `{{API_BASE_URL}}` - API基础URL

## 动态规则
当前环境：{{ENVIRONMENT}}
- 如果是development：可以使用print调试
- 如果是production：必须使用logging
```

### 5.3 最佳实践

#### 5.3.1 DO（推荐做法）

```markdown
# ✅ 好的CLAUDE.md

## 项目信息
- 技术栈：Python 3.11, FastAPI
- 数据库：PostgreSQL
- 缓存：Redis

## 核心规范
1. 类型注解是必须的
2. 使用async/await
3. 错误处理用自定义异常

## 常见任务
### 添加新API
1. 在 src/api/ 创建路由
2. 在 src/services/ 实现逻辑
3. 在 tests/ 添加测试

### 数据库迁移
1. 使用Alembic
2. 命令：alembic revision --autogenerate
```

#### 5.3.2 DON'T（避免做法）

```markdown
# ❌ 不好的CLAUDE.md

## 项目信息
这是一个很复杂的电商系统，使用了很多技术...
（太冗长，缺少结构）

## 规范
- 代码要写得好
- 测试要充分
（太模糊，无法执行）

## 注意事项
不要用print，不要忘记类型注解，不要...
（太多"不要"，应该告诉AI"要做什么"）
```

### 5.4 高级配置

#### 5.4.1 条件性规则

```markdown
# 条件性规则

## 根据文件类型应用不同规范

### Python文件 (*.py)
- 必须有类型注解
- 必须有docstring
- 使用Black格式化

### TypeScript文件 (*.ts)
- 使用ESLint
- 优先使用函数式组件
- 使用strict模式

### Markdown文件 (*.md)
- 使用中文
- 代码块必须指定语言
```

#### 5.4.2 团队共享配置

```markdown
# 团队共享配置

## 提示词模板

### 代码审查模板
```
作为Senior Developer审查以下代码：
1. 架构设计
2. 代码质量
3. 性能问题
4. 安全隐患
```

### 重构模板
```
重构以下代码：
目标：[描述目标]
约束：[列出约束]
验证：[测试方法]
```
```

---

## 第6部分：实战练习（1,000字）

### 练习1：掌握Thinking Mode

**任务**：对比不同思考模式的效果

```bash
# 步骤1：准备测试代码
cat > slow_function.py << 'EOF'
def calculate_sum(numbers):
    total = 0
    for i in range(len(numbers)):
        for j in range(len(numbers)):
            if i == j:
                total += numbers[i]
    return total
EOF

# 步骤2：分别用不同模式优化
# 普通模式
claude -p "优化 slow_function.py"

# 基础思考
claude -p "think 优化 slow_function.py"

# 深度思考
claude -p "think hard 优化 slow_function.py"

# 极限思考
claude -p "think harder 优化 slow_function.py"

# 步骤3：对比结果
# - 普通模式：可能直接给出sum()方案
# - 基础思考：会分析O(n²)问题
# - 深度思考：会对比多种方案的权衡
# - 极限思考：会考虑大数据场景、内存优化等
```

**预期学习**：
- 理解不同思考模式的输出差异
- 学会根据任务复杂度选择合适模式
- 体会深度思考的价值

### 练习2：熟练使用Rewind

**任务**：通过Rewind优化迭代流程

```bash
# 步骤1：启动交互模式
claude

# 步骤2：第一次尝试
You: 重构 user.py 的所有函数

# 步骤3：假设结果不满意，按Escape回退
You: <Escape键>

# 步骤4：给出更精确的指令
You: 只重构 user.py 中的 get_user 函数
     目标：添加缓存支持

# 步骤5：再次回退并优化
You: <Escape键>
You: think 重构 get_user 函数：
     1. 添加Redis缓存
     2. 缓存过期时间5分钟
     3. 缓存key格式：user:{id}
```

**预期学习**：
- 熟练使用Escape键回退
- 学会逐步细化需求
- 理解Rewind的使用场景

### 练习3：构建自动化管道

**任务**：创建一个代码质量检查管道

```bash
#!/bin/bash
# quality_check.sh

PROJECT_DIR=$1

# 阶段1：代码风格检查
echo "=== 阶段1：代码风格检查 ==="
claude -p "
  使用pylint检查 $PROJECT_DIR
  生成报告：style_report.txt
"

# 阶段2：类型检查
echo "=== 阶段2：类型检查 ==="
claude -p "
  使用mypy检查 $PROJECT_DIR
  生成报告：type_report.txt
"

# 阶段3：安全扫描
echo "=== 阶段3：安全扫描 ==="
claude -p "
  使用bandit扫描安全问题
  生成报告：security_report.txt
"

# 阶段4：生成综合报告
echo "=== 阶段4：综合报告 ==="
claude -p "
  综合以上3个报告，生成改进建议
  输出：improvement_plan.md
"

echo "✅ 质量检查完成"
```

**预期学习**：
- 掌握Headless模式
- 学会构建多阶段管道
- 理解自动化工作流的价值

### 练习4：配置多层级CLAUDE.md

**任务**：建立完整的配置层级

```bash
# 步骤1：创建用户级配置
mkdir -p ~/.claude
cat > ~/.claude/CLAUDE.md << 'EOF'
# 我的全局配置
- 使用4空格缩进
- 优先使用函数式编程
- Git提交使用conventional commits
EOF

# 步骤2：创建项目级配置
cat > CLAUDE.md << 'EOF'
# 项目：练习项目
- Python 3.11
- 使用pytest测试
- 使用Black格式化
EOF

# 步骤3：创建模块级配置
mkdir -p src/api
cat > src/api/CLAUDE.md << 'EOF'
# API模块
- 使用FastAPI
- RESTful风格
- 参数验证用Pydantic
EOF

# 步骤4：测试配置生效
cd src/api
claude
You: 创建一个用户API
# 观察Claude是否遵循了3层配置
```

**预期学习**：
- 理解配置层级和优先级
- 学会组织项目配置
- 掌握上下文自动注入

### 练习5：多工作流对比

**任务**：用不同工作流完成同一任务（添加日志功能）

```bash
# 方式1：REPL交互式
claude
You: 为所有API添加请求日志
You: 记录：请求时间、路径、参数、响应时间
You: 使用structlog

# 方式2：Headless单次执行
claude -p "
  为所有API添加请求日志
  要求：
  - 记录请求时间、路径、参数、响应时间
  - 使用structlog
  - 创建中间件统一处理
"

# 方式3：管道式处理
echo "分析API文件" | \
  claude -p "列出所有API路由文件" | \
  claude -p "为这些文件生成日志中间件" | \
  claude -p "应用到所有路由"
```

**预期学习**：
- 理解不同工作流的特点
- 学会根据场景选择工作流
- 掌握多种工作模式

---

## 总结与下一步

### 本节核心要点

1. **交互模式**
   - Thinking Mode四层级：think < think hard < think harder < ultrathink
   - Rewind功能：Escape键回退
   - 会话管理：/export, /resume, /sessions

2. **工作流模式**
   - REPL：探索性开发
   - Headless：自动化任务
   - SDK：程序化集成
   - 管道：多阶段处理

3. **进阶技巧**
   - 结构化提示词
   - 上下文窗口管理
   - 性能优化
   - 团队协作

4. **CLAUDE.md配置**
   - 三层架构：用户级 > 项目级 > 模块级
   - 自动上下文注入
   - 条件性规则

### 下一步学习

- **模块3.1**：MCP集成 - 扩展Claude Code能力
- **模块3.2**：自定义工具开发 - 打造专属AI助手
- **模块3.3**：企业级应用 - CI/CD集成与团队协作

### 推荐资源

- 官方文档：https://docs.anthropic.com/claude-code
- 示例仓库：https://github.com/anthropics/claude-code-examples
- 社区论坛：https://community.anthropic.com

---

**练习提交**：完成以上5个练习后，整理学习笔记并分享你的最佳实践。

**下节预告**：我们将学习如何通过MCP（Model Context Protocol）扩展Claude Code的能力，连接数据库、API、工具等外部资源。

