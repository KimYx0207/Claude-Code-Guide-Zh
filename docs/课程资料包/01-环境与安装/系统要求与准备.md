# Claude Code系统要求与准备

**课程模块**:模块1 - 环境与安装篇
**课程编号**:第1课
**预计学时**:4小时
**难度等级**:⭐ 零基础入门
**更新日期**:2025年12月

---

## 📚 本课学习目标

完成本课学习后,你将能够:

1. **理解Claude Code的核心价值**:掌握Claude Code与传统AI编程工具的本质区别
2. **搭建完整开发环境**:在任意操作系统上成功安装Node.js运行时
3. **配置Anthropic服务**:获取并正确配置API密钥
4. **验证环境可用性**:通过命令行工具确认所有组件正常工作
5. **掌握故障排查技能**:独立解决90%的常见安装问题
6. **建立开发习惯**:理解现代开发工具的基础架构思维

---

## 第一部分:Claude Code简介(为什么选择它?)

### 1.1 什么是Claude Code

Claude Code是Anthropic公司开发的**命令行AI编程助手**。与传统代码编辑器插件不同,它是一个独立运行的CLI(Command Line Interface)工具,通过终端与开发者交互。

**核心特征:**
- **本地优先架构**:代码在你的电脑上处理,不上传到云端
- **全能AI助手**:基于Claude 3.5 Sonnet模型,理解复杂技术需求
- **工具整合能力**:可调用文件操作、终端命令、Web搜索等工具
- **对话式开发**:用自然语言描述需求,AI帮你生成和修改代码

**简单理解:**
把Claude Code想象成一个24小时在线的高级程序员,你用中文或英文告诉他需求,他能帮你写代码、改bug、搜资料、运行测试。但与ChatGPT不同的是,他能直接操作你的代码文件,而不是只生成代码让你复制粘贴。

### 1.2 核心优势:为什么值得学习

#### 优势1:隐私与安全

传统在线AI工具需要你把代码上传到服务器分析。Claude Code不同:
- ✅ 代码文件留在本地,AI只读取你授权的文件
- ✅ 可在企业内网环境使用(配合私有Claude部署)
- ✅ 敏感项目(如金融、医疗系统)也能安全使用

#### 优势2:真正的编程助手

对比其他工具:
- **Copilot**:只能在编辑器内补全代码片段
- **ChatGPT**:需要手动复制粘贴代码
- **Claude Code**:能直接读写文件、运行命令、搜索文档

**实际案例:**
```
你:帮我把项目中所有console.log改成更规范的日志系统
Claude Code:
1. [扫描] 找到37个console.log调用
2. [询问] 是否使用Winston日志库?
3. [执行] 安装依赖、创建配置、批量替换代码
4. [验证] 运行测试确认改动正确
```

#### 优势3:多语言多框架支持

不限于特定技术栈:
- 前端:React、Vue、Next.js
- 后端:Node.js、Python、Go
- 移动端:React Native
- 基础设施:Docker、Kubernetes配置

### 1.3 与主流工具对比

| 对比维度 | Claude Code | Cursor | GitHub Copilot | ChatGPT |
|---------|-------------|--------|----------------|---------|
| **交互方式** | 命令行对话 | IDE集成 | IDE内嵌 | 网页对话 |
| **代码理解** | ⭐⭐⭐⭐⭐ 全项目上下文 | ⭐⭐⭐⭐ 当前文件优先 | ⭐⭐⭐ 当前函数附近 | ⭐⭐ 需手动提供代码 |
| **文件操作** | ✅ 直接读写 | ✅ 通过编辑器 | ❌ 仅建议修改 | ❌ 需手动操作 |
| **工具调用** | ✅ 终端、搜索、git | ⚠️ 部分支持 | ❌ 无 | ❌ 无 |
| **隐私保护** | ⭐⭐⭐⭐⭐ 本地处理 | ⭐⭐⭐ 云端分析 | ⭐⭐ 上传代码 | ⭐ 全部上传 |
| **多任务处理** | ✅ 跨文件重构 | ⚠️ 单文件为主 | ❌ 单行建议 | ❌ 需分步指导 |
| **学习曲线** | 陡峭(需CLI基础) | 平缓(熟悉IDE) | 最简单(自动建议) | 简单(对话即可) |
| **费用** | $20/月(Pro) | $20/月 | $10-19/月 | $20/月(Plus) |

**推荐使用场景:**
- **选Claude Code**:重构遗留项目、跨语言迁移、复杂调试、企业级开发
- **选Cursor**:日常开发、快速原型、学习新框架
- **选Copilot**:函数补全、单元测试生成
- **选ChatGPT**:学习概念、算法解释、非编码技术问题

### 1.4 适合谁学习

**强烈推荐:**
1. **有1年+编程经验的开发者**:能充分利用AI加速工作流
2. **技术Leader/架构师**:需要快速审查和重构代码
3. **独立开发者**:一个人维护多个项目,需要AI协作
4. **开源贡献者**:快速理解陌生代码库

**需要慎重考虑:**
1. **编程零基础**:建议先学基础语法和终端操作(建议学习时长:3-6个月)
2. **只用图形界面**:Claude Code需要熟悉命令行
3. **网络受限**:需要访问Anthropic API(国内需代理)

---

## 第二部分:Node.js环境准备(⭐ 核心重点)

### 2.1 为什么需要Node.js

#### Node.js是什么?

Node.js是一个**JavaScript运行时环境**。简单理解:
- **运行时**:让JavaScript能在服务器或本地电脑上运行(不只是浏览器)
- **环境**:提供文件操作、网络请求等能力

**为什么Claude Code需要它?**
1. Claude Code本身用JavaScript/TypeScript编写
2. 需要npm(Node Package Manager)来安装和更新
3. 许多现代开发工具都依赖Node.js生态

**类比理解:**
就像Windows程序需要.NET Framework,Java程序需要JRE,Claude Code需要Node.js才能运行。

### 2.2 Node.js版本选择(2025年推荐)

#### LTS版本是什么?

LTS = Long Term Support(长期支持版本)
- ✅ 稳定性高,bug少
- ✅ 安全更新持续3年
- ✅ 企业级项目推荐

#### 2025年版本策略

| 版本 | 状态 | 推荐度 | 说明 |
|------|------|--------|------|
| **Node.js 22.x** | 🟢 最新LTS | ⭐⭐⭐⭐⭐ | 2024年10月发布,推荐首选 |
| Node.js 20.x | 🟡 维护期LTS | ⭐⭐⭐⭐ | 稳定但会逐步过时 |
| Node.js 18.x | 🟠 即将结束支持 | ⭐⭐⭐ | 2025年4月停止维护 |
| Node.js 23.x | 🔵 最新特性版 | ⭐⭐ | 不稳定,开发者尝鲜用 |

**本课程统一使用:Node.js 22.x LTS**

#### 如何确认已安装的版本?

打开终端(后面会详细教),输入:
```bash
node -v
```
如果显示`v22.11.0`或`v22.x.x`,说明版本正确。

### 2.3 Windows平台安装详解

#### 步骤1:下载安装包

**官方下载地址:**
```
https://nodejs.org/zh-cn/
```

**操作步骤:**
1. 打开浏览器访问上述网址
2. 点击绿色按钮"下载Node.js (LTS)"
3. 会自动下载`node-v22.x.x-x64.msi`文件(约50MB)

**[截图位置1:Node.js官网首页,标注LTS下载按钮]**

**下载慢怎么办?**
使用国内镜像站(清华大学镜像):
```
https://mirrors.tuna.tsinghua.edu.cn/nodejs-release/v22.11.0/
```
选择`node-v22.11.0-x64.msi`下载。

#### 步骤2:运行安装程序

**详细安装步骤:**

1. **双击`node-v22.x.x-x64.msi`文件**
   - 如果出现"Windows已保护你的电脑",点击"更多信息" → "仍要运行"

2. **欢迎界面**
   - 点击"Next"(下一步)

**[截图位置2:安装向导欢迎界面]**

3. **许可协议**
   - 勾选"I accept the terms in the License Agreement"
   - 点击"Next"

4. **安装路径选择** ⚠️ 重要
   - 默认路径:`C:\Program Files\nodejs\`
   - **建议使用默认路径**(避免权限问题)
   - 如果C盘空间不足,可改为`D:\nodejs\`(但路径中不要有中文)

**[截图位置3:安装路径选择界面,标注路径输入框]**

5. **自定义安装组件**
   - 保持默认全选:
     - ✅ Node.js runtime(运行时)
     - ✅ npm package manager(包管理器)
     - ✅ Online documentation shortcuts(在线文档)
     - ✅ Add to PATH(添加到环境变量) ← 必须勾选!
   - 点击"Next"

**关键说明:**
- "Add to PATH"作用:让你能在任意文件夹打开命令行时使用`node`和`npm`命令
- 如果不勾选,后续需要手动配置环境变量(较复杂)

6. **工具安装(可选)**
   - 出现"Tools for Native Modules"选项
   - 建议**不勾选**(会自动安装Python和Visual Studio构建工具,占用5GB+空间)
   - 如果后续需要编译C++扩展,可通过`npm install --global windows-build-tools`单独安装

7. **开始安装**
   - 点击"Install"
   - 等待3-5分钟(取决于电脑性能)
   - 如果弹出用户账户控制(UAC),点击"是"

8. **完成安装**
   - 看到"Completed"界面
   - 点击"Finish"

**[截图位置4:安装完成界面]**

#### 步骤3:验证安装

**打开命令提示符:**
- 按`Win + R`键
- 输入`cmd`,按回车
- 出现黑色窗口

**[截图位置5:Windows运行对话框输入cmd]**

**执行验证命令:**
```bash
# 检查Node.js版本
node -v

# 预期输出(版本号可能略有不同)
v22.11.0

# 检查npm版本
npm -v

# 预期输出
10.9.0
```

**如果出现错误:**
```bash
'node' 不是内部或外部命令,也不是可运行的程序或批处理文件。
```

**解决方法:**
1. 重启电脑(让环境变量生效)
2. 如果重启后仍有问题,跳到"故障排查"章节的"环境变量手动配置"

#### 步骤4:配置npm国内镜像(可选但推荐)

**为什么需要镜像?**
npm默认从国外服务器下载包,国内访问很慢(几KB/s)。使用淘宝镜像可提速到MB/s级别。

**配置方法:**
```bash
# 设置淘宝镜像
npm config set registry https://registry.npmmirror.com

# 验证配置
npm config get registry

# 应显示
https://registry.npmmirror.com/
```

**测试安装速度:**
```bash
# 安装一个测试包
npm install -g npm-check

# 如果几秒内完成,说明镜像配置成功
```

### 2.4 macOS平台安装详解

macOS用户有两种安装方式:Homebrew(推荐)和官方安装包。

#### 方法1:Homebrew安装(推荐)

**什么是Homebrew?**
macOS的包管理器,类似App Store,但用于安装开发工具。许多专业开发者使用。

**优势:**
- ✅ 一行命令安装
- ✅ 自动配置环境变量
- ✅ 方便管理多个版本
- ✅ 更新简单(`brew upgrade node`)

**前置条件:安装Homebrew**

打开"终端"应用(Spotlight搜索"Terminal"):

```bash
# 检查是否已安装Homebrew
brew -v

# 如果显示版本号,跳过此步骤
# 如果提示"command not found",运行以下命令安装:

/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安装过程约5-10分钟,需要输入电脑密码
```

**安装Node.js:**

```bash
# 安装最新LTS版本
brew install node@22

# 链接到系统路径
brew link node@22

# 验证安装
node -v  # 应显示v22.x.x
npm -v   # 应显示10.x.x
```

**常见问题:**

**问题1:提示权限不足**
```bash
# 修复Homebrew权限
sudo chown -R $(whoami) /usr/local/Homebrew
```

**问题2:brew命令找不到**
```bash
# 手动添加Homebrew到PATH(根据芯片类型选择)

# Intel芯片Mac:
echo 'export PATH="/usr/local/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc

# M1/M2芯片Mac:
echo 'export PATH="/opt/homebrew/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

#### 方法2:官方安装包

**适用场景:**
- 不想使用Homebrew
- 需要固定版本(不自动更新)

**步骤:**

1. **下载安装包**
   - 访问:https://nodejs.org/zh-cn/
   - 点击"下载Node.js (LTS)"
   - 下载`.pkg`文件

2. **运行安装程序**
   - 双击`.pkg`文件
   - 按提示点击"继续"
   - 可能需要输入Mac密码

3. **验证安装**
```bash
# 打开终端
node -v
npm -v
```

**[截图位置6:macOS终端验证Node.js版本]**

#### macOS特有配置

**配置npm全局包路径:**
```bash
# 创建全局包目录
mkdir -p ~/.npm-global

# 配置npm使用该目录
npm config set prefix '~/.npm-global'

# 添加到PATH
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.zshrc
source ~/.zshrc
```

**为什么需要这样配置?**
macOS的系统目录`/usr/local`受保护,直接安装全局包可能需要`sudo`。使用独立目录更安全。

### 2.5 Linux平台安装详解

Linux发行版众多,这里覆盖最常用的Ubuntu/Debian和通用的nvm方式。

#### 方法1:Ubuntu/Debian系统(apt包管理器)

**适用系统:**
- Ubuntu 20.04/22.04/24.04
- Debian 11/12
- Linux Mint

**步骤:**

```bash
# 1. 更新包索引
sudo apt update

# 2. 安装Node.js 22.x官方源
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -

# 3. 安装Node.js和npm
sudo apt install -y nodejs

# 4. 验证安装
node -v   # v22.x.x
npm -v    # 10.x.x

# 5. 安装构建工具(编译原生模块时需要)
sudo apt install -y build-essential
```

**国内用户加速:**
```bash
# 使用清华大学镜像源
curl -fsSL https://mirrors.tuna.tsinghua.edu.cn/nodesource/deb_22.x/setup | sudo -E bash -
sudo apt install -y nodejs
```

#### 方法2:nvm方式(推荐高级用户)

**什么是nvm?**
Node Version Manager,可以在同一台电脑安装多个Node.js版本,随时切换。

**优势:**
- ✅ 多版本管理(项目A用Node 18,项目B用Node 22)
- ✅ 不需要sudo权限
- ✅ 适合测试不同版本

**安装nvm:**
```bash
# 下载并安装nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash

# 重启终端或运行
source ~/.bashrc

# 验证nvm安装
nvm -v
```

**使用nvm安装Node.js:**
```bash
# 查看可用的LTS版本
nvm ls-remote --lts

# 安装Node.js 22 LTS
nvm install 22

# 设为默认版本
nvm alias default 22

# 验证
node -v
```

**切换版本示例:**
```bash
# 安装多个版本
nvm install 20
nvm install 22

# 切换到Node 20
nvm use 20
node -v  # v20.x.x

# 切换回Node 22
nvm use 22
node -v  # v22.x.x
```

#### 其他Linux发行版

**CentOS/RHEL/Fedora:**
```bash
# 添加Node.js 22仓库
curl -fsSL https://rpm.nodesource.com/setup_22.x | sudo bash -

# 安装
sudo yum install -y nodejs
```

**Arch Linux:**
```bash
# Arch的官方源通常有最新版本
sudo pacman -S nodejs npm
```

---

## 第三部分:系统要求检查

### 3.1 操作系统兼容性

#### 支持的操作系统

| 操作系统 | 最低版本 | 推荐版本 | 说明 |
|---------|---------|---------|------|
| **Windows** | Windows 10 | Windows 11 | 需要64位系统 |
| **macOS** | macOS 10.15 Catalina | macOS 13+ Ventura | 支持Intel和Apple Silicon |
| **Linux** | 内核3.10+ | 5.x+ | 主流发行版均可 |

#### 不支持的环境

❌ Windows 7/8(Node.js 22不支持)
❌ 32位操作系统
❌ Windows ARM64(可运行但性能差)
❌ 虚拟机内存<4GB

### 3.2 硬件要求

#### 最低配置

- **CPU**:双核处理器(Intel i3或同等性能)
- **内存**:4GB RAM
- **存储**:至少2GB可用空间
- **网络**:稳定互联网连接(访问Anthropic API)

#### 推荐配置

- **CPU**:四核及以上(i5/i7/Ryzen 5/M1+)
- **内存**:8GB+ RAM(处理大型项目时16GB更佳)
- **存储**:SSD固态硬盘,10GB+可用空间
- **网络**:10Mbps+宽带,低延迟(<200ms到Anthropic服务器)

#### 性能影响说明

**为什么需要较好配置?**
1. **Node.js运行时**:占用200-500MB内存
2. **Claude AI响应**:需要网络传输代码上下文(可能数MB)
3. **大型项目分析**:扫描上千个文件需要CPU和内存
4. **并发操作**:同时运行测试、构建、AI查询

**实际测试数据:**
```
配置          | 启动时间 | 大项目分析 | 内存占用
-------------|---------|-----------|----------
i3 + 4GB     | 8秒     | 45秒      | 600MB
i5 + 8GB     | 3秒     | 12秒      | 800MB
i7 + 16GB    | 2秒     | 5秒       | 1.2GB
M1 + 16GB    | 1秒     | 3秒       | 900MB
```

### 3.3 网络环境要求

#### 必需的网络连接

**API访问要求:**
- **目标服务器**:api.anthropic.com
- **协议**:HTTPS (443端口)
- **延迟**:建议<500ms
- **带宽**:上传1Mbps,下载2Mbps

**测试连接:**
```bash
# Windows PowerShell
Test-Connection api.anthropic.com

# macOS/Linux
ping api.anthropic.com

# 测试HTTPS连接
curl -I https://api.anthropic.com
```

#### 国内网络特殊说明

**问题:**
Anthropic服务器在海外,国内直连可能很慢或无法访问。

**解决方案:**

**方案1:使用代理**
```bash
# 设置HTTP代理
export HTTP_PROXY=http://127.0.0.1:7890
export HTTPS_PROXY=http://127.0.0.1:7890

# Windows PowerShell
$env:HTTP_PROXY="http://127.0.0.1:7890"
$env:HTTPS_PROXY="http://127.0.0.1:7890"
```

**方案2:配置Claude Code使用代理**
```bash
# 在~/.bashrc或~/.zshrc添加
export ANTHROPIC_API_PROXY="http://your-proxy:port"
```

**方案3:企业内网**
如果公司提供海外专线,无需额外配置。

#### 防火墙配置

**企业防火墙可能阻止的端口:**
- 443 (HTTPS)
- 80 (HTTP重定向)

**解决方法:**
联系IT部门,申请白名单添加:
```
api.anthropic.com
cdn.anthropic.com
```

### 3.4 终端环境配置

#### Windows终端选择

**推荐选项(按优先级):**

1. **Windows Terminal** ⭐⭐⭐⭐⭐
   - 微软官方现代终端
   - 支持多标签、Unicode、主题
   - Microsoft Store免费下载
   - **推荐所有Windows用户使用**

2. **PowerShell 7** ⭐⭐⭐⭐
   - 跨平台PowerShell版本
   - 比内置PowerShell 5.1更强大
   - 下载:https://github.com/PowerShell/PowerShell

3. **Git Bash** ⭐⭐⭐
   - 模拟Linux Bash环境
   - 随Git for Windows安装
   - 适合习惯Linux的用户

4. **CMD(命令提示符)** ⭐⭐
   - Windows内置
   - 功能有限,不推荐长期使用

**安装Windows Terminal:**
```
1. 打开Microsoft Store
2. 搜索"Windows Terminal"
3. 点击"获取"安装
4. 设为默认终端:
   设置 → 启动 → 默认终端应用程序 → Windows Terminal
```

**[截图位置7:Windows Terminal界面展示]**

#### macOS终端配置

**内置终端已足够:**
macOS自带的Terminal.app功能完善,无需额外安装。

**可选升级:iTerm2**
- 更多主题和快捷键
- 分屏功能更强
- 下载:https://iterm2.com

**推荐Shell:zsh**
macOS 10.15+默认使用zsh(比bash更现代)

**配置优化:**
```bash
# 安装Oh My Zsh(美化和插件管理)
sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

# 推荐插件(编辑~/.zshrc)
plugins=(git node npm)
```

#### Linux终端

大多数发行版自带的Bash或zsh终端已满足需求。

**可选增强:**
```bash
# 安装Zsh
sudo apt install zsh

# 设为默认Shell
chsh -s $(which zsh)
```

---

## 第四部分:Anthropic账号准备

### 4.1 注册Anthropic账号

#### 注册流程

**步骤1:访问注册页面**
```
https://console.anthropic.com/
```

点击"Sign Up"(注册)。

**[截图位置8:Anthropic Console登录页面]**

**步骤2:选择注册方式**

三种方式任选其一:
1. **Google账号登录**(推荐,最快)
2. **邮箱+密码注册**
3. **GitHub账号登录**

**推荐使用Google账号原因:**
- ✅ 一键注册,无需验证邮箱
- ✅ 自动同步个人信息
- ✅ 支持两步验证(2FA)

**步骤3:完善账号信息**

注册后需要填写:
- **姓名**:真实姓名或开发者昵称
- **使用场景**:选择"Personal Use"(个人使用)或"Business"(商业)
- **主要编程语言**:可多选

**步骤4:手机验证(可能需要)**

部分地区注册时需要验证手机号:
- 支持中国大陆号码(+86)
- 会收到6位数验证码短信
- 如果未收到,可选择语音验证

**注意:**
国内手机号注册成功率约80%。如果多次失败,可尝试:
- 使用Google Voice虚拟号码
- 使用香港/台湾号码
- 联系Anthropic支持

### 4.2 API Key获取步骤

#### 什么是API Key?

API Key是一串密钥,作用类似密码,用于:
- 证明你有权使用Claude AI服务
- 追踪API调用次数(计费依据)
- 控制访问权限

**格式示例:**
```
sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

#### 获取步骤

**步骤1:进入API Keys页面**

登录后点击左侧菜单:
```
Settings → API Keys
```

或直接访问:
```
https://console.anthropic.com/settings/keys
```

**[截图位置9:API Keys管理页面]**

**步骤2:创建新Key**

点击"Create Key"按钮,填写:
- **Key名称**:例如"claude-code-laptop"(方便区分多个key)
- **权限**:选择"Full Access"(完全访问)

**步骤3:复制并保存Key**

⚠️ **关键警告:**
- Key只显示一次!关闭窗口后无法再看到
- 必须立即复制并保存到安全位置
- 不要分享给任何人

**保存方法:**
1. 创建文本文件`anthropic-key.txt`
2. 粘贴完整key
3. 保存到电脑安全位置(如Documents文件夹)

**步骤4:验证Key有效性**

使用curl命令测试:
```bash
# macOS/Linux
curl https://api.anthropic.com/v1/messages \
  -H "x-api-key: 你的API_KEY" \
  -H "anthropic-version: 2023-06-01" \
  -H "content-type: application/json" \
  -d '{
    "model": "claude-3-5-sonnet-20241022",
    "max_tokens": 1024,
    "messages": [{"role": "user", "content": "Hello"}]
  }'

# 如果返回JSON响应(而不是错误),说明Key有效
```

**Windows PowerShell测试:**
```powershell
$headers = @{
    "x-api-key" = "你的API_KEY"
    "anthropic-version" = "2023-06-01"
    "content-type" = "application/json"
}
$body = '{"model":"claude-3-5-sonnet-20241022","max_tokens":1024,"messages":[{"role":"user","content":"Hello"}]}'
Invoke-RestMethod -Uri "https://api.anthropic.com/v1/messages" -Method POST -Headers $headers -Body $body
```

### 4.3 环境变量配置

#### 什么是环境变量?

环境变量是操作系统级别的配置,让程序可以读取敏感信息(如API Key),而不需要写在代码里。

**好处:**
- ✅ 安全:不会意外提交到GitHub
- ✅ 灵活:不同电脑可用不同Key
- ✅ 标准:所有开发工具都支持

#### Windows配置方法

**临时配置(仅当前终端有效):**
```powershell
# PowerShell
$env:ANTHROPIC_API_KEY="sk-ant-api03-你的key"

# CMD
set ANTHROPIC_API_KEY=sk-ant-api03-你的key
```

**永久配置(推荐):**

**方法1:通过图形界面**
```
1. 右键"此电脑" → 属性
2. 点击"高级系统设置"
3. 点击"环境变量"
4. 在"用户变量"区域点击"新建"
5. 变量名:ANTHROPIC_API_KEY
6. 变量值:sk-ant-api03-你的完整key
7. 点击"确定"保存
8. 重启所有终端窗口
```

**[截图位置10:Windows环境变量配置界面]**

**方法2:通过PowerShell**
```powershell
# 永久添加用户环境变量
[System.Environment]::SetEnvironmentVariable('ANTHROPIC_API_KEY', 'sk-ant-api03-你的key', 'User')

# 验证
$env:ANTHROPIC_API_KEY
```

#### macOS/Linux配置方法

**编辑Shell配置文件:**

```bash
# 确定使用的Shell
echo $SHELL

# 如果是bash,编辑~/.bashrc
# 如果是zsh(macOS默认),编辑~/.zshrc

# 添加以下行(替换为真实Key)
export ANTHROPIC_API_KEY="sk-ant-api03-你的key"

# 保存后重新加载
source ~/.zshrc  # 或 source ~/.bashrc

# 验证
echo $ANTHROPIC_API_KEY
```

**使用nano编辑器示例:**
```bash
# 打开配置文件
nano ~/.zshrc

# 添加Key配置
export ANTHROPIC_API_KEY="sk-ant-api03-xxxxx"

# 保存:Ctrl+O → 回车 → Ctrl+X退出
```

#### 安全注意事项

**✅ 正确做法:**
- 只在本地配置环境变量
- 不要提交`.env`文件到Git
- 定期轮换API Key

**❌ 错误做法:**
- 把Key直接写在代码里:`const key = "sk-ant-..."`
- 在GitHub Issues/论坛公开Key
- 与他人共享Key

**泄露后的处理:**
1. 立即到Console删除泄露的Key
2. 创建新Key
3. 更新环境变量
4. 检查API使用记录是否异常

### 4.4 计费与订阅

#### Claude Code需要付费吗?

**两层计费:**

1. **Claude Code工具本身**
   - ✅ 免费开源
   - 无需购买License

2. **Claude API使用费**
   - ⚠️ 按调用量计费
   - 类似手机话费(用多少付多少)

#### Claude API价格(2025年)

| 模型 | 输入价格 | 输出价格 | 说明 |
|------|---------|---------|------|
| **Claude 3.5 Sonnet** | $3 / 1M tokens | $15 / 1M tokens | 主力模型,平衡性能和成本 |
| Claude 3 Opus | $15 / 1M tokens | $75 / 1M tokens | 最强模型,处理复杂任务 |
| Claude 3 Haiku | $0.25 / 1M tokens | $1.25 / 1M tokens | 最便宜,简单任务可用 |

**Token是什么?**
- 1 token ≈ 0.75个英文单词
- 1 token ≈ 1-2个汉字
- 1000 tokens ≈ 750字短文

**实际成本估算:**
```
场景:用Claude Code编写一个中型功能(500行代码)
- 读取项目上下文:20K tokens(输入)
- AI分析和规划:5K tokens(输出)
- 生成代码:10K tokens(输出)

总成本 = (20K × $3 + 15K × $15) / 1,000,000
       = ($0.06 + $0.225)
       = $0.285 ≈ 2元人民币
```

**月度预算参考:**
- **轻度使用**(每周2-3次):$5-10/月
- **日常开发**(每天使用):$20-50/月
- **重度使用**(全天候):$100+/月

#### 免费额度

**新用户福利:**
注册Anthropic账号后,通常可获得:
- $5免费额度(约可生成2万行代码)
- 有效期3个月

**查看额度:**
```
Console → Settings → Billing → Credits
```

#### 付费订阅

**个人开发者方案:**
- 按实际用量计费(Pay-as-you-go)
- 需要绑定信用卡
- 支持Visa/Mastercard/American Express

**企业方案:**
- 批量折扣
- 专属支持
- 联系sales@anthropic.com

**国内支付解决方案:**
1. 使用虚拟信用卡(如Dupay、Nobepay)
2. 通过第三方代充服务(需谨慎选择正规平台)
3. 使用双币信用卡(需支持海外支付)

---

## 第五部分:故障排查

### 5.1 Node.js安装问题

#### 问题1:安装后命令无法识别

**症状:**
```bash
node -v
# 输出:'node' 不是内部或外部命令
```

**原因:**
环境变量PATH未正确配置。

**解决方法(Windows):**
```
1. 找到Node.js安装路径(默认C:\Program Files\nodejs\)
2. 复制路径
3. 右键"此电脑" → 属性 → 高级系统设置 → 环境变量
4. 在"系统变量"中找到"Path",双击编辑
5. 点击"新建",粘贴Node.js路径
6. 确定保存
7. 重启所有终端窗口
```

**验证方法:**
```bash
# 查看PATH变量
echo %PATH%

# 应该包含类似:C:\Program Files\nodejs\
```

#### 问题2:权限错误(macOS/Linux)

**症状:**
```bash
npm install -g some-package
# Error: EACCES: permission denied
```

**原因:**
默认全局目录需要管理员权限。

**解决方法:**
```bash
# 不要使用sudo!改用用户目录
mkdir -p ~/.npm-global
npm config set prefix '~/.npm-global'

# 添加到PATH
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.bashrc
source ~/.bashrc

# 重新尝试安装
npm install -g some-package
```

#### 问题3:版本冲突

**症状:**
```bash
node -v
# v18.20.0  # 不是期望的v22.x
```

**原因:**
系统中存在多个Node.js版本。

**解决方法:**

**Windows:**
```bash
# 卸载旧版本
控制面板 → 程序和功能 → 卸载Node.js

# 重新安装v22
```

**macOS(使用Homebrew):**
```bash
# 查看已安装版本
brew list | grep node

# 卸载旧版本
brew uninstall node@18

# 安装新版本
brew install node@22
brew link node@22 --force
```

**Linux(使用nvm):**
```bash
# 列出所有版本
nvm list

# 切换到v22
nvm use 22

# 设为默认
nvm alias default 22
```

### 5.2 网络连接问题

#### 问题1:无法访问api.anthropic.com

**测试方法:**
```bash
ping api.anthropic.com

# 如果显示"请求超时"或"找不到主机"
```

**解决方法:**

**检查DNS:**
```bash
# 临时改用Google DNS
# Windows(管理员PowerShell)
Set-DnsClientServerAddress -InterfaceAlias "以太网" -ServerAddresses ("8.8.8.8","8.8.4.4")

# macOS/Linux
sudo nano /etc/resolv.conf
# 添加:nameserver 8.8.8.8
```

**配置代理:**
```bash
# 假设本地有Clash在7890端口
export https_proxy=http://127.0.0.1:7890
export http_proxy=http://127.0.0.1:7890

# 测试
curl -I https://api.anthropic.com
```

#### 问题2:npm下载速度慢

**症状:**
```bash
npm install
# 下载速度仅10KB/s,经常超时
```

**解决方法:**
```bash
# 切换到国内镜像
npm config set registry https://registry.npmmirror.com

# 验证
npm config get registry

# 如果依然慢,清除缓存
npm cache clean --force
```

#### 问题3:SSL证书错误

**症状:**
```bash
npm install
# Error: unable to verify the first certificate
```

**临时解决(不推荐长期使用):**
```bash
npm config set strict-ssl false
```

**正确解决:**
```bash
# 更新系统根证书
# Windows:通过Windows Update
# macOS:无需手动更新
# Linux:
sudo apt update && sudo apt upgrade ca-certificates
```

### 5.3 API Key配置问题

#### 问题1:环境变量未生效

**症状:**
```bash
echo $ANTHROPIC_API_KEY
# 显示为空
```

**解决方法:**

**确认配置文件:**
```bash
# macOS/Linux
cat ~/.zshrc | grep ANTHROPIC
# 应该看到:export ANTHROPIC_API_KEY="sk-ant-..."

# 如果没有,手动添加
echo 'export ANTHROPIC_API_KEY="你的key"' >> ~/.zshrc
source ~/.zshrc
```

**Windows:**
```powershell
# 检查是否配置
[System.Environment]::GetEnvironmentVariable('ANTHROPIC_API_KEY', 'User')

# 如果为空,重新配置
[System.Environment]::SetEnvironmentVariable('ANTHROPIC_API_KEY', 'sk-ant-api03-你的key', 'User')

# 重启PowerShell
```

#### 问题2:Key无效或过期

**症状:**
```bash
curl测试API返回:
{
  "error": {
    "type": "authentication_error",
    "message": "invalid x-api-key"
  }
}
```

**解决方法:**
```
1. 登录 console.anthropic.com
2. Settings → API Keys
3. 检查Key是否被删除或禁用
4. 如果无效,创建新Key
5. 更新环境变量
```

#### 问题3:Key格式错误

**症状:**
Key看起来不完整或有空格。

**正确格式检查:**
```bash
# Key应该满足:
# 1. 以"sk-ant-api03-"开头
# 2. 后面跟长串字母数字
# 3. 总长度约95字符
# 4. 无空格、无换行

# 验证长度
echo -n $ANTHROPIC_API_KEY | wc -c
# 应该输出:95左右
```

### 5.4 终端相关问题

#### 问题1:中文乱码

**Windows CMD乱码:**
```bash
# 临时切换到UTF-8
chcp 65001

# 永久设置
reg add HKCU\Console /v CodePage /t REG_DWORD /d 65001 /f
```

**PowerShell中文显示:**
```powershell
# 设置输出编码
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
```

#### 问题2:命令历史记录丢失

**保存命令历史:**

**Bash/Zsh:**
```bash
# 确认历史记录文件
echo $HISTFILE

# 应该是~/.bash_history或~/.zsh_history

# 如果为空,添加到配置
echo 'export HISTFILE=~/.zsh_history' >> ~/.zshrc
echo 'export HISTSIZE=10000' >> ~/.zshrc
```

**PowerShell:**
```powershell
# 安装PSReadLine(提供历史记录)
Install-Module -Name PSReadLine -Force

# 添加到Profile
notepad $PROFILE
# 添加:Import-Module PSReadLine
```

#### 问题3:复制粘贴不工作

**Windows Terminal:**
```
右键选中文本自动复制
Ctrl+Shift+C 复制
Ctrl+Shift+V 粘贴
```

**macOS Terminal:**
```
Cmd+C 复制
Cmd+V 粘贴
```

**Linux终端:**
```
Ctrl+Shift+C 复制
Ctrl+Shift+V 粘贴
# 或鼠标中键粘贴
```

---

## 实战练习

### 练习1:完整环境验证

**目标:**
确认所有组件正常工作。

**步骤:**
```bash
# 1. 检查Node.js
node -v
npm -v

# 2. 检查网络连接
curl -I https://api.anthropic.com

# 3. 检查环境变量
echo $ANTHROPIC_API_KEY  # macOS/Linux
echo %ANTHROPIC_API_KEY%  # Windows CMD
$env:ANTHROPIC_API_KEY   # Windows PowerShell

# 4. 测试npm镜像速度
time npm view react version
# 应该在5秒内完成
```

**验收标准:**
- ✅ Node.js版本>=22.0.0
- ✅ API能成功访问(返回200或401)
- ✅ 环境变量显示完整Key
- ✅ npm命令响应快速

### 练习2:创建测试项目

**目标:**
验证Node.js环境能正常运行项目。

**步骤:**
```bash
# 1. 创建项目目录
mkdir ~/claude-test
cd ~/claude-test

# 2. 初始化npm项目
npm init -y

# 3. 安装测试依赖
npm install express

# 4. 创建简单服务器
cat > server.js << 'EOF'
const express = require('express');
const app = express();
app.get('/', (req, res) => {
  res.send('Node.js环境测试成功!');
});
app.listen(3000, () => {
  console.log('服务器运行在 http://localhost:3000');
});
EOF

# 5. 运行服务器
node server.js

# 6. 新开终端测试
curl http://localhost:3000
```

**验收标准:**
- ✅ npm install成功完成
- ✅ 服务器启动无错误
- ✅ 浏览器或curl能访问到"测试成功"消息

### 练习3:API Key功能验证

**目标:**
确认API Key能正常调用Claude。

**步骤:**

创建测试脚本`test-api.js`:
```javascript
const https = require('https');

const data = JSON.stringify({
  model: 'claude-3-5-sonnet-20241022',
  max_tokens: 100,
  messages: [{
    role: 'user',
    content: '用一句话介绍Claude Code'
  }]
});

const options = {
  hostname: 'api.anthropic.com',
  port: 443,
  path: '/v1/messages',
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'x-api-key': process.env.ANTHROPIC_API_KEY,
    'anthropic-version': '2023-06-01'
  }
};

const req = https.request(options, (res) => {
  let body = '';
  res.on('data', (chunk) => body += chunk);
  res.on('end', () => {
    console.log('API响应:', JSON.parse(body));
  });
});

req.on('error', (e) => {
  console.error('请求失败:', e.message);
});

req.write(data);
req.end();
```

运行测试:
```bash
node test-api.js
```

**验收标准:**
- ✅ 收到JSON格式的AI响应
- ✅ 响应中包含content字段
- ✅ 没有authentication_error错误

---

## 总结与检查清单

完成本课后,请确认以下所有项:

**环境安装:**
- [ ] Node.js 22.x安装成功
- [ ] npm命令可用
- [ ] 配置了npm国内镜像(国内用户)

**账号准备:**
- [ ] 注册了Anthropic账号
- [ ] 获取了API Key
- [ ] Key已保存到安全位置

**系统配置:**
- [ ] 环境变量ANTHROPIC_API_KEY已配置
- [ ] 终端能正确显示中文
- [ ] 网络能访问api.anthropic.com

**功能验证:**
- [ ] 通过练习1的环境验证
- [ ] 通过练习2的项目测试
- [ ] 通过练习3的API调用

**如果以上全部勾选,恭喜你已经完成准备工作!**

下一课我们将学习:[02-Claude Code安装与配置.md]

---

## 附录

### A. 常用命令速查

```bash
# Node.js版本管理
node -v                    # 查看Node版本
npm -v                     # 查看npm版本
nvm list                   # 列出所有Node版本(nvm用户)

# npm配置
npm config list            # 查看所有配置
npm config get registry    # 查看镜像源
npm cache clean --force    # 清除缓存

# 环境变量查看
echo $ANTHROPIC_API_KEY    # macOS/Linux
echo %ANTHROPIC_API_KEY%   # Windows CMD
$env:ANTHROPIC_API_KEY     # PowerShell

# 网络测试
ping api.anthropic.com     # 测试连通性
curl -I https://api.anthropic.com  # 测试HTTPS访问
```

### B. 推荐学习资源

**Node.js官方文档:**
- 英文:https://nodejs.org/docs/
- 中文:https://nodejs.cn/

**Anthropic API文档:**
- https://docs.anthropic.com/

**终端使用教程:**
- Windows Terminal:https://learn.microsoft.com/zh-cn/windows/terminal/
- macOS终端:https://support.apple.com/zh-cn/guide/terminal/

**社区支持:**
- Claude Discord:https://discord.gg/anthropic
- GitHub Discussions:https://github.com/anthropics/claude-code/discussions

---

**课程制作**:[你的名字]
**最后更新**:2025年12月
**版本**:v1.0
**许可**:本课程采用CC BY-NC-SA 4.0许可